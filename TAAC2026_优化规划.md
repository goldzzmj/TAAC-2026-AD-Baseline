# TAAC2026 优化规划 — 系统化消融实验方案

> 创建时间: 2026-05-09 | 最后更新: 2026-05-21 (QRY-1/DOM-1/TEMP-1/U-D2 结果更新, T2 仍最佳 0.844955)
> 当前最佳: Phase1-T2-cosine-lr, 推理 AUC = **0.844955**
> 原始基线: 第三次-HyFormer (v3), 推理 AUC = **0.839805**

---

## 目录

1. [当前状态与核心发现](#1-当前状态与核心发现)
2. [竞赛社区关键情报](#2-竞赛社区关键情报)
3. [数据集深度分析](#3-数据集深度分析)
4. [历史实验经验总结](#4-历史实验经验总结)
5. [消融实验方法论](#5-消融实验方法论)
6. [优化方向一: 数据与特征工程](#6-优化方向一-数据与特征工程)
7. [优化方向二: 模型结构微调](#7-优化方向二-模型结构微调)
8. [优化方向三: 训练策略优化](#8-优化方向三-训练策略优化)
9. [优化方向四: 训练加速与工程优化](#9-优化方向四-训练加速与工程优化)
10. [优化方向五: 验证协议修正](#10-优化方向五-验证协议修正)
11. [优化方向六: 后处理与模型导出](#11-优化方向六-后处理与模型导出)
12. [优化方向七: 缩小 Valid→Eval AUC Gap](#12-优化方向七-缩小-valideval-auc-gap)
13. [UniRec 参考项目分析](#13-unirec-参考项目分析)
14. [实验执行路线图](#14-实验执行路线图)
15. [实验记录](#15-实验记录)
16. [全面负向分析与新优化方向](#16-全面负向分析与新优化方向-🆕)

---

## 1. 当前状态与核心发现

### 1.1 AUC 排行榜 (有效评估, AUC > 0.5)

| 排名 | 版本 | 推理 AUC | 训练 Valid AUC | Gap | 阶段 |
|------|------|----------|---------------|-----|------|
| **1** | **Phase1-T2-cosine-lr** | **0.844955** | 0.8658 | -2.08% | 控制变量实验 |
| 2 | Phase2-T6a-FeatMask (ep4) | 0.844911 | 0.8660 | -2.11% | 控制变量实验 |
| 3 | Phase2-T6a-FeatMask (ep5) | 0.844416 | 0.8660 | -2.16% | 控制变量实验 |
| **4** | **Phase5.5-QRY1-TargetAwareQuery (ep5)** | **0.844115** | 0.8651 | -2.10% | YAO 架构优化 |
| 5 | Phase4-U-D1-DenseProj-v2 | 0.843732 | ~0.865 | -2.15% | 数据与特征工程 |
| 6 | Phase3-ACCEL-BF16-v2 (ep5) | 0.84327 | 0.8663 | -2.30% | 训练加速 |
| **7** | **Phase5.5-DOM1-DomainGate-v2 (ep4)** | **0.843100** | 0.8648 | -2.17% | YAO 架构优化 |
| **8** | **Phase5.5-TEMP1-TemporalGate (ep5)** | **0.843035** | 0.8650 | -2.20% | YAO 架构优化 |
| 9 | Phase4-SEQ2a-TimePeriod (ep4) | 0.842944 | 0.8665 | -2.39% | 数据与特征工程 |
| 10 | Phase2-T5-Dropout | 0.842873 | 0.8659 | -2.30% | 控制变量实验 |
| 11 | Phase2-T9-LongTrain (ep6) | 0.842832 | ~0.8659 | -2.31% | 控制变量实验 |
| 12 | Phase2-T4b-LS (ep6) | 0.84164 | 0.8662 | -2.46% | 控制变量实验 |
| 13 | Phase3-ACCEL-BF16-v2 (ep4) | 0.841327 | 0.8663 | -2.50% | 训练加速 |
| 14 | Phase3.5-GAP4-SWA (ep4) | 0.840984 | - | - | 训练策略 |
| 15 | Phase5-MLP1-DeepClassifier | 0.840612 | ~0.863 | -2.52% | 模型微调 |
| 16 | 第三次-HyFormer (v3) | 0.839805 | 0.8636 | -2.38% | 原始迭代 |
| 17 | Phase0-timesplit-deterministic | 0.838662 | 0.8655 | -2.68% | 控制变量实验 |
| 18 | 第二次-HyFormer-5-epoch | 0.837729 | - | - | 原始迭代 |
| 19 | Phase4-U-D2-SharedFID (ep4) | 0.836210 | 0.8650 | -2.88% | 数据与特征工程 |
| 20 | Phase4-SEQ1-fid47Treatment (ep4) | 0.8375 | - | - | 数据与特征工程 |

> *更新时间: 2026-05-21，QRY-1/DOM-1/TEMP-1/U-D2 评估结果全部负向，T2 仍为最佳*

### 1.2 控制变量实验进度

```
Phase 0: 验证修正 + 种子固定 ───── 完成 ✅
Phase 1: 训练策略消融 ─────────── 完成 ✅
Phase 2: 基于 T2 的进一步消融 ─── 全部完成 ✅
├── P2-T3: EMA 0.999     → ❌ KILLED (训练极慢)
├── P2-T4b: LS 0.01      → ❌ 完成 (Eval 0.8416, 负向 -0.34%, Gap 增大)
├── P2-T5: Dropout 0.02  → ❌ 完成 (Eval 0.8429, 负向 -0.21%)
├── P2-T6a: FeatMask 0.05→ ❌ 完成 (Eval 0.8449, 接近但略低)
└── P2-T9: 更长训练       → ❌ KILLED (Eval 0.8428, 负向 -0.21%)
Phase 3: 训练加速 (并行进行) 🔄
├── ACCEL-Compile v1      → ❌ Segfault (CUDA Graphs 不兼容)
├── ACCEL-Compile v2      → 🔄 训练中 (ep2, Valid 0.8641)
├── ACCEL-BF16-v1         → ❌ FAILED (TypeError: BFloat16 .numpy())
└── ACCEL-BF16-v2         → ❌ 完成 (ep5 Eval 0.8433, 负向 -0.17%, 不可用于竞赛)
Phase 4: 数据与特征工程 ✅ (U-D1 完成)
├── SEQ-2a: 时间周期       → ❌ ep4 Eval 0.8429, ep7 Eval 0.84009, ep8 Eval 0.8381, 全负向
├── SEQ-1: 移除 seq_c fid47 → ❌ Eval 0.8375, 严重负向 -0.75% (fid47 是关键特征, 不可移除)
├── U-D1: Dense MLP       → ❌ Eval 0.843732, -0.12% vs T2, 轻负向
└── U-D2: Shared-FID 联合  → ❌ Eval 0.836210, -0.87% vs T2, 严重负向
Phase 5: 模型微调 (谨慎) 🔄 (MLP1/M2/M3 完成)
├── MLP-1: Classifier 加深 → ❌ Eval 0.840612, -0.43% vs T2, 负向
├── M-2: HyFormer Blocks 1 → ⚠️ 训练完成, Best Valid 0.8654 (ep7), 权重未保存无评估
├── M-3: HyFormer Blocks 3 → ⚠️ 训练完成, Best Valid 0.8655 (ep4), 权重未保存无评估
└── DIN-1: 前端 DIN Query 增强 → 待做
Phase 5.5: YAO 洞察驱动架构优化 🆕 (全部负向)
├── QRY-1: Target-Aware Query → ❌ Eval 0.844115 (ep5), -0.08% vs T2, 接近但负向
├── DOM-1: 域级动态门控       → ❌ Eval 0.843100 (ep4), -0.19% vs T2, 负向
└── TEMP-1: 时间条件化门控     → ❌ Eval 0.843035, -0.19% vs T2, 负向
Phase 3.5: 训练策略优化 🆕
├── GAP4-SWA: epoch4 → ⚠️ Eval 0.840984, -0.40% vs T2, 负向
```

### 1.3 关键突破

**T2 (Cosine LR) 仍是最佳**:
- 推理 AUC 从 v3 的 0.8398 → **0.844955**, 提升 **+0.61%**
- Valid AUC 基本持平 (0.8658 vs 0.8655 Phase0)
- Gap 从 2.38% 缩小到 2.08%
- **说明**: LR scheduler 对推理泛化有显著帮助，即使训练 AUC 未提升

**Phase4-U-D1 (2-layer MLP) 接近最佳但略低**:
- Eval AUC = **0.843732** (evalTaskId: 94348)
- 对比 T2: -0.12% (0.844955 → 0.843732)
- Valid AUC: 0.86421 vs T2 0.8658 (-0.16%)
- **分析**: 918→128→64 渐进压缩理论上更优，但实际效果不如单层投影
- **可能原因**: 增加的参数量 (~126K) 导致过拟合

**Phase5-MLP1 (3-layer Classifier) 显著负向**:
- Eval AUC = **0.840612** (evalTaskId: 94315)
- 对比 T2: -0.43% (0.844955 → 0.840612)
- Valid AUC: 0.86308 vs T2 0.8658 (-0.29%)
- Gap 增大: 2.52% vs T2 2.08%
- **分析**: bottleneck 结构在分类任务中不适用

**Phase4-SEQ1 (移除 seq_c fid47) 严重负向**:
- Eval AUC = **0.8375** (epoch 4)
- 对比 T2: -0.75% (0.844955 → 0.8375)
- **分析**: fid47 虽与 itemid 相同（ID 类特征），但在 seq_c 序列中提供关键的 target-item matching 信号
- **结论**: fid47 不可移除，序列中的 ID 特征有其独特价值

**T6a (Feature Masking) 接近但略低**:
- Best Valid AUC = **0.8660** (比 T2 的 0.8658 高 +0.02%)
- Eval AUC = 0.8449 (epoch 4) / 0.8444 (epoch 5) — 略低于 T2 的 0.844955
- Feature Masking 对 Valid AUC 有微弱正向，但推理泛化略差

### 1.4 当前 Baseline 配置 (Phase0 + T2, 冻结)

```
# Phase0 基础配置
batch_size=64         lr=1e-4           sparse_lr=0.05
dropout=0.01          seq_encoder=transformer   seq_top_k=50
rankmixer             user_ns_tokens=5  item_ns_tokens=2
num_queries=2         num_blocks=2      d_model=64
valid_ratio=0.05      time_split=True   seed=42 (deterministic)

# T2 增量配置
lr_scheduler=cosine   warmup_steps=500  min_lr_ratio=0.1
num_epochs=8          patience=3

# 默认关闭（已证明有效但需独立验证）
reinit_sparse_after_epoch=1, reinit_cardinality_threshold=0
无 EMA, 无 label_smoothing, 无 feature_mask
```

---

## 2. 竞赛社区关键情报

> 来源: 竞赛论坛选手 "Seed" 分享 (242赞, 369收藏)

### 2.1 该选手的建模方案

1. **User dense feats**: 将 `ue` 分离出来，其他跟 int 特征 pair 加权
2. **模型架构**: raw embedding + 序列 merge + fafe + din + mlp
3. **时间特征补充**
4. **全程 vibe coding**

### 2.2 关键发现

| 发现 | 详细说明 | 对我们的影响 |
|------|---------|-------------|
| **ID 特征无用** | uid 在训练集和测试集都是只有一条 instance 且无交集; item_id 线下 AUC 虚高，线上掉分 | 需要识别并处理 ID 类特征，避免过拟合 |
| **随机种子不固定** | cuda deterministic 没固定 + sdpa 算子 + iterable 问题 | 影响实验可比性，Phase0 已修复 |
| **验证集划分** | 应按 timestamp 划分，目前先按随机划分实验 | Phase0 已修正为时间切分 |
| **Pure model 建模** | rankmixer/onetrans/hyformer/mixformer 等纯模型方案都掉分，**可能数据量不够** | 模型结构改动风险高，应聚焦数据和特征 |
| **长序列无效** | seqc_feat47 跟 itemid 一样，做 match 特征掉分 | seq_c 的 fid47 特征应特殊处理 |

### 2.3 YAO 的战略洞察 (2026-05-20 新增)

> 来源: 竞赛前列选手 "YAO" 的经验分享

**核心思想**: 不要停留在"哪个 trick 能上分"，而应反过来思考：**一个工业推荐模型到底应该怎样组织多域行为、时间漂移和特征交互**。

#### 洞察 1: Query Token 作为接口 (Interface)

> "让 query token 作为非序列特征、行为序列和时间上下文之间的接口，逐层从不同用户行为域里解码证据"

**当前模型问题**:
- Query token 由 `NS_tokens + MeanPool(Seq_i)` 生成，**不包含 target item 信息**
- Target 信息仅在后端 DIN (TargetAttentionPooling) 阶段才引入
- HyFormer blocks 内部做 cross-attention 时对 target 完全无感知
- 这等于让模型在"不知道要预测什么"的情况下做序列建模

**优化方向**: 让 Query token 从一开始就融合 target-aware 信息，成为连接三者的桥梁
- 非序列特征 (user_int, user_dense, item_int)
- 行为序列 (seq_a/b/c/d 四个域)
- 时间上下文 (timestamp, time_bucket)

**对应实验**: QRY-1 (见 7.4 节)

#### 洞察 2: 多域行为可靠性判断 (Domain Reliability)

> "根据当前 query、上下文和时间信息判断哪个行为域更可靠"

**当前模型问题**:
- 4 个行为域 (seq_a/b/c/d) 被独立编码后简单拼接
- 没有机制让模型动态判断"这次预测应该更依赖哪个行为域"
- 例如：用户在看新闻时，广告点击行为 (seq_a) 可能比展示行为 (seq_b) 更有参考价值

**优化方向**: 添加域级动态门控机制，让模型根据 query 和上下文选择最可靠的行为域
- **对应实验**: DOM-1 (见 7.5 节)

#### 洞察 3: 时间漂移建模 (Temporal Drift)

> "关注 temporal drift 而非单纯特征交互"

**当前模型问题**:
- SEQ2a 尝试添加绝对时间特征 (hour_of_day + day_of_week)，结果全负向
- 但 SEQ2a 的失败不代表"时间信息无用"——问题在于我们添加了**错误的时间特征**
- 真正的 temporal drift 是指：特征重要性随时间变化（而非用户的日内/周模式）
- 例如：某个广告在促销期间效果好，促销结束后特征重要性就变了

**优化方向**: 建模时间对特征重要性的调节效应，而非添加绝对时间特征
- **对应实验**: TEMP-1 (见 7.6 节)

#### 洞察 4: 反方审稿人思维 (方法论)

> "这个提升有没有可能是过拟合？是不是只在 A 榜有效？是不是增加了复杂度但没有稳定收益？"

**应用于我们的实验回顾**:
- T2 的 Valid AUC 仅比 Phase0 高 0.03%，但 Eval AUC 高出 0.63% — 这说明 **Valid AUC 不是可靠信号**
- 所有负向实验 (T4b/T5/T6a/T9) 都是 Valid AUC ≥ T2 但 Eval AUC < T2 — **过拟合验证集**
- 这意味着我们应该：选择能缩小 Gap 的方案，而非提升 Valid AUC 的方案
- **新指标关注**: 优先关注 Gap 缩小，而非 Valid AUC 绝对值

---

## 3. 数据集深度分析

### 3.1 特征全景

```
┌─────────────────────────────────────────────────────────┐
│                    样本特征结构                           │
├──────────────┬──────────────────────────────────────────┤
│  User 侧     │ user_int_feats (多 fid)                  │
│              │ user_dense_feats (10 fid, dim=918)        │
│              │ 4 个行为序列域 (seq_a/b/c/d)              │
├──────────────┼──────────────────────────────────────────┤
│  Item 侧     │ item_int_feats (多 fid)                  │
│              │ item_dense_feats = 空 (dim=0)             │
├──────────────┼──────────────────────────────────────────┤
│  时间信息     │ timestamp (当前曝光时间)                  │
│              │ 序列内历史时间戳 (per domain)              │
│              │ time_bucket (非均匀分桶, 65 类)            │
└──────────────┴──────────────────────────────────────────┘
```

### 3.2 User Dense 特征分析

**总维度: 918** — 被投影为**单个 NS token** (d_model=64)，信息压缩率高达 **14:1**

**优化空间**:
- 8 个 shared-fid 特征的 int/dense 部分应该**联合建模**而非独立处理
- 918 维压缩到 64 维损失严重，社区方案建议**将 ue 分离出来单独处理**
- 对 shared-fid 特征可以做 int×dense 的交叉特征

### 3.3 Item 特征分析

**当前状态: Item Dense 完全为空** (dim=0)

**关键发现 (来自社区情报)**:
- **ID 特征 (可能包含 fid=47) 线下虚高但线上掉分**
- item_id 分布在 train/test 之间不一致
- `seqc_feat47` 跟 `itemid` 是一样的

### 3.4 特征问题诊断

| 问题 | 影响特征 | 严重度 | 建议 |
|------|---------|--------|------|
| User Dense 压缩率过高 (14:1) | user_dense 全部 918 维 | 高 | 分离 ue 或分层投影 |
| Shared-fid 未联合建模 | fid 62-66, 89-91 | 高 | 做 int×dense 交叉 |
| NS 分组未启用 | user/item 全部 int | 中 | 启用 ns_groups.json |
| ID 特征线下虚高 | 可能含 item_id 类 fid | 高 | 识别并降低权重/去除 |
| Item Dense 缺失 | - | 低 | 无法补充 |
| 时间特征单一 | 仅 time_bucket | 中 | 补充 log-age, recency |

---

## 4. 历史实验经验总结

### 4.1 全部实验结果汇总

#### 早期迭代 (非控制变量)

| 版本 | 推理 AUC | Valid AUC | 对比 v3 | 改动点 | 结果 |
|------|----------|-----------|---------|--------|------|
| v3 (baseline) | 0.839805 | 0.863607 | — | 基础 HyFormer | 最佳(早期) |
| v4 | 0.834840 | 0.864813 | **-0.497%** | +EMA +Pairwise +LS(0.05) +ImprovedClassifier | 负优化 |
| v4e-首次 | 0.830585 | - | -0.922% | +ResClassifier | 更差 |
| v4e-新模型 | 0.835595 | 0.864935 | -0.421% | v4e 模型配置 | 略有恢复但仍差 |
| v5 | N/A | N/A | 失败 | +AMP +Longer +RoPE +ffn_only +大batch | 训练崩溃 |
| v6 | 待评估 | 0.861518 | ? | +EMA +FeatMask(0.1) +LS(0.01) +CosineLR | Valid AUC 低于 Phase0 |

#### 控制变量实验 (Phase 0/1/2)

| 编号 | 实验名 | Valid AUC | Eval AUC | 对比 T2 | 唯一改动 | 状态 |
|------|--------|-----------|----------|---------|---------|------|
| Phase0 | 时间切分+种子固定 | 0.8655 | 0.8387 | -0.63% | +time_split +deterministic_seed | ✅ 完成 |
| T1a | 关闭 sparse re-init | — | — | — | reinit_epoch=-1 | ❌ OOM |
| T1b | 选择性 sparse re-init | 0.8620 | 待评估 | — | epoch=2, thresh=100k | ❌ 负向 |
| **T2** | **Cosine LR** | **0.8658** | **0.8450** | **baseline** | cosine, warmup=500 | ✅ **最佳** |
| P2-T3 | EMA 0.999 | — | — | — | T2 + EMA(0.999) | ❌ KILLED |
| P2-T4b | LS 0.01 | **0.8662** | 0.84164 | -0.34% | T2 + LS(0.01) | ❌ 完成 (负向) |
| P2-T5 | Dropout 0.02 | 0.8659* | 待获取 | ? | T2 + dropout=0.02 | 🔄 ep6 |
| P2-T6a | FeatMask 0.05 | **0.8660** | **0.8449** | **-0.01%** | T2 + featmask(0.05) | ✅ 完成 |
| P2-T9 | 更长训练 | ~0.8659 | 0.8428 | -0.21% | T2 + epochs=12, patience=5 | ❌ KILLED (负向) |

> *带 * 号为训练中间结果

### 4.2 负面经验 (不要做的)

| 改动 | 影响方向 | 结论 |
|------|---------|------|
| Pairwise BPR Loss (0.1) | v4 下降 0.5% | 排序损失不适合此数据 |
| Improved Classifier | v4 下降 | 增加复杂度无收益 |
| Dropout 从 0.01 → 0.05 | v4 下降 | 过强正则 |
| Label Smoothing 0.05 | v4 下降 | 过强平滑 |
| AMP/BF16 + ffn_only + 大 batch | v5 失败 | 组合改动太多，无法定位问题 |
| rankmixer mode=ffn_only | v5 失败 | 可能信息丢失严重 |
| 关闭 sparse re-init (T1a) | CUDA OOM | 显存不足 |
| 选择性 sparse re-init (T1b) | Valid AUC 下降 0.35% | Epoch 2 reinit 后 AUC 暴跌 |
| v6 多项改动同时加入 | Valid AUC 低于 Phase0 | EMA+LS+FeatMask+CosineLR 组合不如单独 CosineLR |
| **EMA (decay=0.999)** | T3 训练极慢被 KILLED | CPU-GPU 数据搬运开销大，不可用 |
| **Feature Masking 0.05** | T6a Eval AUC 0.8449 < T2 的 0.8450 | 微弱负向，不纳入 baseline |
| **Dropout 0.02 (从 0.01 提升)** | T5 Eval AUC 0.8429 < T2 的 0.8450 (-0.21%) | 更强 dropout 增大泛化 Gap，当前 0.01 已是最优 |
| **更长训练 (epochs=12, patience=5)** | T9 Eval AUC 0.8428 < T2 的 0.8450 (-0.21%) | Cosine LR 下 8 epochs 已是最优，更多 epoch 导致过拟合 |
| **Label Smoothing 0.01** | T4b Eval AUC 0.84164 < T2 的 0.8450 (-0.34%) | Valid AUC 略高 (+0.04%) 但 Eval AUC 显著下降，Gap 从 2.08% 增至 2.46%。v4 的 LS 0.05 也负向，LS 整体不可用 |
| **BF16 混合精度** | BF16-v2 Eval AUC 0.841327 (ep4) / 0.84327 (ep5) < T2 的 0.8450 | BF16 精度损失导致 AUC 下降，不可用于竞赛。可用于加速实验迭代但不用于正式提交 |
| **时间周期特征 (SEQ2a)** | SEQ2a ep4 Eval 0.8429, ep7 Eval 0.8401, ep8 Eval 0.8381 < T2 的 0.8450 | hour_of_day + day_of_week sinusoidal 编码未带来正向收益，且 epoch 越后 Eval AUC 越低，说明过拟合严重。用户日内/周行为模式对该任务影响有限 |
| **移除 seq_c fid47 (SEQ1)** | SEQ1 ep4 Eval 0.8375 < T2 的 0.8450 (-0.75%) | fid47 是 seq_c 中极其重要的特征，移除后 AUC 暴跌。虽然 fid47 与 itemid 相同（ID 类特征），但它在序列建模中提供了不可替代的 target-item matching 信号，不可移除 |
| **HyFormer Blocks 1 (M2)** | M2 Best Valid AUC 0.8654 < T2 的 0.8658 (-0.04%) | 减少 Block 数量导致表示能力不足。⚠️ 权重未保存无法评估推理，但 Valid AUC 负向 |
| **HyFormer Blocks 3 (M3)** | M3 Best Valid AUC 0.8655 < T2 的 0.8658 (-0.03%) | 增加 Block 数量过拟合加剧 (ep4 后未再提升)。⚠️ 权重未保存无法评估推理，但 Valid AUC 负向 |

### 4.3 正面经验 (已验证有效)

| 改动 | 证据 | 可信度 |
|------|------|--------|
| **Cosine LR + Warmup 500** | T2 推理 AUC +0.52% (0.8398→0.8450) | **高 (控制变量验证)** |
| 时间切分验证 | Phase0 完成, 建立可靠基线 | 高 |
| 确定性种子固定 | Phase0 完成, 保证可复现 | 高 |
| RankMixer NS tokenizer | v3 vs 早期版本提升明显 | 高 |
| Target Attention (DIN) | 模型核心组件 | 高 |
| Cross Fusion (DCN-V2) | 模型核心组件 | 高 |
| 5 epoch > 1 epoch | 第二次-HyFormer 从 0.8267→0.8377 | 高 |
| 默认 sparse re-init | Phase0 Valid AUC 0.8655 vs T1b 的 0.8420 | 高 (不改动更优) |

### 4.4 Phase2 新发现总结

| 实验 | Valid AUC | Eval AUC | 结论 | 分析 |
|------|-----------|----------|------|------|
| T3-EMA | — | — | ❌ 不可用 | 训练极慢（CPU-GPU shadow 同步），被平台 KILLED |
| T6a-FeatMask | 0.8660 (+0.02%) | 0.8449 (-0.01%) | ❌ 微弱负向 | Valid 略高但 Eval 略低，说明 featmask 增加了泛化 gap |
| T4b-LS | 0.8662 (+0.04%) | 0.84164 (-0.34%) | ❌ 负向 | Valid AUC 略高但 Eval AUC 显著下降，Gap 从 2.08% 增至 2.46% |
| T5-Dropout | 0.8659 (+0.01%) | 0.8429 (-0.21%) | ❌ 负向 | Valid 略高但 Eval 显著下降，dropout 增大 Gap |
| T9-Long | ~0.8659 (ep6) | 0.8428 (-0.21%) | ❌ 负向 | 更多 epoch (12) 导致过拟合，8 epochs 已最优 |

**关键洞察**:
1. **Feature Masking 正则化效果有限** — 5% mask 对泛化帮助不大
2. **EMA 不适合当前场景** — CPU shadow 同步开销导致训练速度严重下降
3. **Dropout 0.02 负向** — Valid AUC 略高但 Eval AUC 下降 0.21%，Gap 从 2.08% 增至 2.30%
4. **更长训练负向** — 12 epochs 比 8 epochs 差，Cosine LR 下的 epoch 数已最优
5. **Label Smoothing 0.01 负向** — Valid AUC 略高 (+0.04%) 但 Eval AUC 显著下降 (-0.34%)，Gap 从 2.08% 增至 2.46%
6. **v4 负优化的根因确认** — EMA (训练慢) + FeatMask (微弱负向) + Dropout (负向) + LS (负向) 四项全部负向
7. **Phase2 核心结论**: 更强的正则化（Dropout/FeatMask/LS）和更长训练都增大了泛化 Gap，当前 T2 的配置已是训练策略的最优平衡。Phase2 全部 5 个实验均为负向或不可用
8. **SEQ2a 时间周期特征负向** — ep4 Eval 0.8429, ep7 Eval 0.8401, ep8 Eval 0.8381，所有 epoch 均低于 T2。hour_of_day + day_of_week sinusoidal 编码无效，且训练越久过拟合越严重
9. **SEQ1 移除 fid47 严重负向** — ep4 Eval 0.8375 (-0.75%)，fid47 是 seq_c 中极其重要的特征。虽然社区情报称"seqc_feat47 跟 itemid 一样，做 match 特征掉分"，但我们的实验表明**完全移除**该特征会导致更严重的性能下降。说明 fid47 在序列建模中的 attention/match 机制是模型性能的关键组成部分

---

## 5. 消融实验方法论

### 5.1 核心原则: 控制变量

```
消融实验 = 固定 baseline 配置 + 仅改变一个变量 + 对比推理 AUC

变量 X:  [选项 A]  [选项 B]  [选项 C]
         ↓         ↓         ↓
        base+Xa   base+Xb   base+Xc
         ↓         ↓         ↓
     eval_AUC   eval_AUC   eval_AUC
```

### 5.2 实验纪律

1. **每次只改一个变量** — 禁止同时改多个配置
2. **所有实验基于当前最佳配置** — 除非某个改动已证明有效并纳入新 baseline
3. **必须提交推理评估** — 训练 Valid AUC 不够，必须看推理 AUC
4. **记录完整配置** — 每次实验记录 run.sh 的完整参数
5. **AUC 变化 < 0.05% 视为噪声** — 不做结论，需要重复验证

### 5.3 当前 Baseline 冻结清单 (Phase0 + T2)

```
ns_tokenizer_type=rankmixer     user_ns_tokens=5       item_ns_tokens=2
num_queries=2                   num_hyformer_blocks=2   d_model=64
seq_encoder_type=transformer    seq_top_k=50            use_rope=False
rank_mixer_mode=full            batch_size=64           grad_accum_steps=1
lr=1e-4                         sparse_lr=0.05          dropout_rate=0.01
dense_weight_decay=0.01         num_epochs=8            patience=3
valid_ratio=0.05                time_split=True         seed=42
lr_scheduler=cosine             warmup_steps=500        min_lr_ratio=0.1
reinit_sparse_after_epoch=1     reinit_cardinality_threshold=0
无 EMA, 无 label_smoothing, 无 feature_mask
```

---

## 6. 优化方向一: 数据与特征工程

> **优先级: 最高** — 训练策略已接近极限，特征工程是下一阶段突破口

### 6.1 User Dense 特征优化 (预计影响: +0.2% ~ +0.5%)

**问题**: 918 维 user_dense 压缩为 1 个 token (64 维)，信息损失巨大

#### 实验 U-D1: User Dense 分层投影

| 变量 | 值 |
|------|-----|
| 改动 | user_dense 投影从 1 token → 2-3 tokens |
| 方法 | 将 918 维分成 2-3 段，分别投影 |
| 约束 | d_model % T == 0 需要满足 |

#### 实验 U-D2: Shared-FID 联合建模

| 变量 | 值 |
|------|-----|
| 改动 | 对 shared-fid (62-66, 89-91) 的 int 和 dense 部分**联合建模** |
| 方法 | int_emb × dense_value 或 concat 后投影 |

#### 实验 U-D3: User Dense 特征归一化

| 变量 | 值 |
|------|-----|
| 改动 | 对 user_dense 做 z-score 或分位截断 |
| 方法 | 统计训练集 dense 特征均值/方差，做标准化 |

### 6.2 Item 特征优化

#### 实验 IT-1: ID 特征处理

| 变量 | 值 |
|------|-----|
| 改动 | 识别并处理 ID 类特征 |
| 方法 A | 将 ID 特征的 emb_skip_threshold 设为 0 (跳过) |
| 方法 B | 对 ID 特征降低 lr 或加强 weight_decay |
| 方法 C | 使用 hash bucket 替代直接 embedding |

**社区情报**: ID 特征线下虚高但线上掉分，这是**高风险高收益**的优化点

### 6.3 序列特征优化

#### 实验 SEQ-1: seq_c fid47 特殊处理

| 变量 | 值 |
|------|-----|
| 改动 | seqc_feat47 与 itemid 相同，可能引入过拟合 |
| 方法 A | 去除 seq_c 的 fid47 特征 |
| 方法 B | 将 fid47 的 vocab_size 限制在较低值 |

**社区情报**: "seqc_feat47 跟 itemid 是一样的，做不做 match 特征都掉分"

**方法 A 实验结果**: ❌ 严重负向
- 移除 seq_c fid47 后 Eval AUC = 0.8375 (vs T2 的 0.844955, -0.75%)
- **结论**: fid47 虽与 itemid 相同，但在序列建模中提供了关键的 target-item matching 信号，完全移除会导致性能暴跌
- **方法 B 仍可尝试**: 限制 vocab_size 可能是更好的策略，而非完全移除

#### 实验 SEQ-2: 时间特征增强 — 按周/日/小时分解

> **现状分析**: 当前仅使用 time-delta bucket（`current_time - event_time` 的非均匀分桶），编码"多久前发生"，但不编码"何时发生"。原始 timestamp 已在 dataset.py 提取但**未送入模型**（仅用于计算 time-delta）。

**当前时间特征处理流程**:
```
Parquet: timestamp(全局) + {domain}_{ts_fid}(历史时间戳)
  → time_diff = current_time - historical_time (≥0)
  → searchsorted(BUCKET_BOUNDARIES) → 65 个桶 (0=padding, 1..64)
  → nn.Embedding(65, d_model) 加到 sideinfo projection
  → 缺失: 无绝对时间信息（小时/星期几/是否周末）
```

**优化方案**: 从全局 `timestamp` 和序列历史时间戳中提取周期性时间特征

| 特征 | 来源 | 编码方式 | 预期作用 |
|------|------|---------|---------|
| `hour_of_day` | `timestamp % 86400 // 3600` → 0-23 | Sinusoidal: `[sin(h*2π/24), cos(h*2π/24)]` | 捕捉用户日内活跃模式（凌晨/午间/晚间） |
| `day_of_week` | `(timestamp // 86400) % 7` → 0-6 | Sinusoidal: `[sin(d*2π/7), cos(d*2π/7)]` | 捕捉工作日/周末差异 |
| `is_weekend` | `day_of_week >= 5` | Binary embedding (0/1) | 广告转化率的周末效应 |
| `hour_bin` | 将 24h 分为 6 个时段 | Embedding(7, D) (0=padding, 1-6=时段) | 粗粒度时段模式，减少参数 |

**实验设计 (控制变量)**:

| 实验 | 唯一改动 | 复杂度 |
|------|---------|--------|
| SEQ-2a: 全局时间周期 | 在 ModelInput 中新增 `time_features`，从全局 timestamp 提取 hour_of_day + day_of_week | 低 |
| SEQ-2b: 序列事件时间周期 | 从序列历史时间戳提取每个事件的 hour/day，作为序列 token 的附加 embedding | 中 |
| SEQ-2c: 时间间隔精细化 | 在现有 bucket 基础上增加 log-age 连续特征 | 低 |

**实现要点**:
1. `dataset.py`: 在 `_convert_batch` 中从 `timestamps` 提取 hour/day，添加到 output dict
2. `model.py`: 新增 `time_period_embedding` 模块，与 HyFormer output 拼接后送入 classifier
3. 保持向后兼容：通过 `--use_time_period` flag 控制，默认关闭

### 6.4 数据特征实验优先级排序

| 优先级 | 实验编号 | 实验名称 | 预期收益 | 风险 | 实现难度 |
|--------|---------|---------|---------|------|---------|
| ~~P0~~ | ~~SEQ-2a~~ | ~~全局时间周期 (hour+day)~~ | ~~+0.1~0.3%~~ | ~~低~~ | ~~低~~ | ❌ 全负向 (ep4/7/8 均低于 T2)
| P0 | U-D1 | User Dense MLP 投影 | +0.2~0.5% | 低 | 中 | 🔄 v2 训练中 (ep4 Valid 0.8642, 待评估)
| P0 | SEQ-1 | seq_c fid47 处理 | +0.1~0.3% | 低 | 低 | ❌ 严重负向 (Eval 0.8375, -0.75%) |
| P1 | U-D2 | Shared-FID 联合建模 | +0.2~0.4% | 低 | 中 |
| P1 | IT-1 | ID 特征处理 | +0.1~0.5% | 中 | 中 |
| P1 | MLP-1 | Classifier 加深 | +0.05~0.15% | 低 | 低 | 🔄 训练中 (ep2 Valid 0.8631, 待评估)
| P2 | SEQ-2b | 序列事件时间周期 | +0.1~0.2% | 低 | 中 |
| P2 | SEQ-2c | 时间间隔精细化 | +0.0~0.1% | 低 | 低 |
| P3 | U-D3 | Dense 归一化 | +0.0~0.1% | 低 | 低 |

---

## 7. 优化方向二: 模型结构微调

> **优先级: 低** — 社区情报和我们的经验都表明纯模型改动风险高

### 7.1 模型改动经验教训

```
v3 (Transformer)                → 0.8398  ★
v4 (+EMA+Pairwise+LS+Improved)  → 0.8348  ↓
v5 (Longer+RoPE+ffn_only+AMP)   → 失败    ✗
v6 (EMA+FeatMask+LS+CosineLR)   → Valid 0.8615 低于 Phase0
T2 (仅 +CosineLR)               → 0.8450  ★★★
T6a (+FeatMask 0.05)            → 0.8449  ≈ (微弱负向)
T3 (+EMA)                       → KILLED  ✗ (训练太慢)
```

**结论**: 模型结构改动风险高，训练策略中 LR scheduler 收益最大

### 7.2 DIN+MLP 深度整合方案

> **现状分析**: 当前 DIN (TargetAttentionPooling) 仅作为 HyFormer backbone 后的附加模块，通过 gated residual 融入。HyFormer 内部的 query token 由 `NS_tokens + MeanPool(Seq)` 生成，**不包含目标 item 信息**。这是"事后补救"式使用 DIN，而非"前端感知"式。

**当前架构流水线**:
```
NS tokens + MeanPool(Seq) → Query Generator → HyFormer blocks (无 target 感知)
    → output_proj → [DIN target attn] → [context fusion] → [cross fusion] → classifier
                           ↑
                     item_context (mean of item NS tokens)
```

#### 实验 DIN-1: 前端 DIN Query 增强

| 变量 | 值 |
|------|-----|
| 改动 | 在 Query Generator 中用 DIN-style target attention 替换 MeanPool |
| 方法 | `Query_i = FFN(NS_flat + DIN(Seq_i, item_context))` 替代 `FFN(NS_flat + MeanPool(Seq_i))` |
| 预期 | 让 HyFormer blocks 内部的 cross-attention 就能感知目标 item，提前引入 target-aware 信息 |
| 风险 | 中 — 改动 query generator 核心，需要确保不影响已有训练稳定性 |

**技术细节**:
- 在 `MultiSeqQueryGenerator.__init__` 中为每个 domain 添加一个轻量 DIN pooling:
  ```python
  # 每个 domain 的 target-conditioned pooling
  self.din_pool = nn.Sequential(
      nn.Linear(4 * d_model, d_model), nn.SiLU(), nn.Linear(d_model, d_model)
  )
  ```
- 在 `forward` 中替换 `mean_pool(seq)` 为 `din_pool(seq, item_context)`
- 需要将 `item_context` 传递到 query generator 中（当前不传递）

#### 实验 DIN-2: 多头 Target Attention

| 变量 | 值 |
|------|-----|
| 改动 | 将单头 DIN activation 扩展为多头 |
| 方法 | 使用 K=4 个独立 activation head，每个 head 独立做 target-conditioned pooling |
| 预期 | 捕捉用户兴趣的多方面（品牌偏好、价格区间、类别偏好等） |
| 风险 | 低 — 输出维度不变，仅内部扩展 |

#### 实验 MLP-1: 分类器 MLP 加深

| 变量 | 值 |
|------|-----|
| 改动 | 将 classifier 从 2 层扩展为 3 层，加入 expansion bottleneck |
| 方法 | `Linear(64→128) → LN → SiLU → Dropout → Linear(128→64) → LN → SiLU → Linear(64→1)` |
| 预期 | 更好地利用融合后的特征表示 |
| 风险 | 低 — 仅改 classifier，不影响 backbone |

**当前 classifier**: `Linear(64→64) → LN → SiLU → Dropout(0.01) → Linear(64→1)` (仅 ~4.3K 参数)

#### 实验 MLP-2: DIN 输出 + HyFormer 输出 拼接后分类

| 变量 | 值 |
|------|-----|
| 改动 | 将 DIN output 和 HyFormer output 拼接后送入更宽的 MLP |
| 方法 | `concat(hyformer_out, din_out)` → `Linear(128→64) → ... → Linear(64→1)` |
| 预期 | 让 classifier 同时看到 sequence-level 和 target-conditioned 信息 |
| 风险 | 中 — 需要确保 DIN 和 HyFormer 的梯度不会互相干扰 |

### 7.3 模型实验优先级排序

| 优先级 | 实验编号 | 实验名称 | 预期收益 | 风险 | 实现难度 |
|--------|---------|---------|---------|------|---------|
| P1 | MLP-1 | Classifier 加深 | +0.05~0.15% | 低 | 低 | 🔄 训练中 (ep2 Valid 0.8631, 待评估)
| P2 | DIN-1 | 前端 DIN Query 增强 | +0.1~0.3% | 中 | 中 |
| P2 | MLP-2 | DIN+HyFormer 拼接分类 | +0.05~0.15% | 中 | 中 |
| P3 | M-1 | HyFormer Block 数量 | +0.0~0.1% | 低 | 低 (仅改超参) |
| P3 | DIN-2 | 多头 Target Attention | +0.0~0.15% | 低 | 中 |
| P4 | M-4 | Cross Fusion 配置 | +0.0~0.1% | 低 | 低 |

> **重要约束**: 社区情报表明 "pure model 建模方案都掉分"，模型改动风险高。优先做 MLP-1 (classifier 加深，风险最低)，再考虑 DIN-1。

### 7.4 YAO 洞察驱动的架构级优化 (2026-05-20 新增)

> **动机**: 之前所有实验都在 T2 baseline 上做"微调"（参数、正则、特征），但 YAO 的洞察指出问题在**架构层面**——query token 没有做好"接口"的角色。

#### 实验 QRY-1: Target-Aware Query Token Interface

| 变量 | 值 |
|------|-----|
| 改动 | 在 Query Generator 中用 target-conditioned pooling 替换 MeanPool |
| 核心思想 | 让 HyFormer blocks 内部做 cross-attention 时就已经感知 target item |
| 方法 | `Query_i = FFN(NS_flat + TargetAwarePool(Seq_i, item_context))` |
| 替代 | `MeanPool(Seq_i)` → `Softmax(Seq_i · item_context) · Seq_i` (轻量 DIN pooling) |
| 预期 | HyFormer 内部 cross-attention 从"盲目搜索"变为"目标导向搜索" |
| 风险 | 中 — 改动 query generator，但输出维度不变 |

**当前 Query Generator 流水线**:
```
NS tokens (flat) + MeanPool(seq_domain_i) → FFN → Query_i (d_model)
```
- `MeanPool` 无差别地平均所有序列 token，丢失了 target 相关信息
- HyFormer blocks 用这个 "盲 Query" 做 cross-attention，效率低

**QRY-1 改进**:
```
NS tokens (flat) + DIN_pool(seq_domain_i, item_context) → FFN → Query_i (d_model)
```
- `DIN_pool` 让 Query 从一开始就包含 target-aware 信号
- HyFormer blocks 内部 cross-attention 变得 target-aware
- 实现简洁：每个 domain 添加一个 `Linear(4*d_model, d_model)` 的 DIN activation unit

**与 DIN-1 的区别**:
- DIN-1 是在 HyFormer 输出后添加 DIN pooling（后置感知）
- QRY-1 是在 Query Generator 阶段就注入 target 信息（前置感知）
- 两者不冲突，可以叠加使用

**实现要点**:
1. `MultiSeqQueryGenerator.__init__` 添加 `din_activation` per domain
2. `forward` 接收 `item_context` 参数
3. 替换 `mean_pool(seq)` 为 `din_activation(seq, item_context)`

#### 实验 DOM-1: 域级动态可靠性门控

| 变量 | 值 |
|------|-----|
| 改动 | 在 HyFormer 输出后添加域级 attention gate，动态加权各行为域的贡献 |
| 核心思想 | 不同预测样本应该依赖不同的行为域 |
| 方法 | `domain_weight = Softmax(FFN([domain_out_1; domain_out_2; ...] · query))` |
| 预期 | 模型学会根据 query 和上下文选择最可靠的行为域 |
| 风险 | 低 — 输出维度不变，仅添加域级权重 |

**当前域处理**:
```
seq_a → encoder → out_a (B, T, D)
seq_b → encoder → out_b (B, T, D)
seq_c → encoder → out_c (B, T, D)
seq_d → encoder → out_d (B, T, D)
→ concat → HyFormer blocks (无差别处理)
```

**DOM-1 改进**:
```
seq_a → encoder → out_a ──→ gate_a ──→ weighted_out_a
seq_b → encoder → out_b ──→ gate_b ──→ weighted_out_b   → concat → HyFormer
seq_c → encoder → out_c ──→ gate_c ──→ weighted_out_c
seq_d → encoder → out_d ──→ gate_d ──→ weighted_out_d

gate = Softmax(FFN(domain_out) · item_context)  ← 用 item_context 做 query
```

**实现要点**:
1. 添加 `DomainGate` 模块：`nn.Linear(d_model, 1)` per domain
2. 在 domain output 送入 HyFormer 之前计算 gate weights
3. `weighted_out = out * sigmoid(gate_score)`
4. 保留 residual：`final_out = weighted_out + (1-gate) * mean_pool(out)`

#### 实验 TEMP-1: 时间条件化特征门控

| 变量 | 值 |
|------|-----|
| 改动 | 用 timestamp embedding 对 user_dense token 做条件化门控 |
| 核心思想 | 建模时间对特征重要性的调节效应（temporal drift） |
| 方法 | `gate = sigmoid(FFN(time_bucket_emb))`, `enhanced_dense = user_dense * gate` |
| 预期 | 不同时间窗口下，dense 特征的重要性自适应调整 |
| 风险 | 低 — 仅在 user_dense token 上加门控，不影响其他模块 |

**与 SEQ2a 的区别**:
- SEQ2a 添加了**绝对时间特征** (hour_of_day, day_of_week) — 这假设用户行为有固定的日内/周模式
- TEMP-1 用时间信息**调制已有特征** — 这假设特征重要性随时间漂移
- SEQ2a 失败说明用户行为没有固定周期模式，但不代表时间信息无用
- TEMP-1 的方式更符合 YAO 说的 "temporal drift" 建模

**实现要点**:
1. 从已有 `time_bucket` embedding 获取时间表示
2. 添加 `TemporalGate`: `nn.Sequential(nn.Linear(d_model, d_model), nn.Sigmoid())`
3. `user_dense_tok = user_dense_tok * temporal_gate(time_emb) + user_dense_tok`

### 7.5 YAO 洞察实验优先级排序

| 优先级 | 实验编号 | 实验名称 | 预期收益 | 风险 | 实现难度 | 理由 |
|--------|---------|---------|---------|------|---------|------|
| **P0** | **QRY-1** | **Target-Aware Query** | **+0.2~0.5%** | 中 | 中 | YAO 核心洞察，架构级改动，直接解决 "盲 query" 问题 |
| **P1** | **DOM-1** | **域级动态门控** | **+0.1~0.3%** | 低 | 中 | 低风险，补充 QRY-1 的域级感知 |
| **P2** | **TEMP-1** | **时间条件化门控** | **+0.05~0.15%** | 低 | 低 | 最简单，低风险，可独立验证 |
| P2 | DIN-1 | 前端 DIN Query 增强 | +0.1~0.3% | 中 | 中 | 与 QRY-1 互补，可叠加 |

> **关键决策**: QRY-1 优先级最高，因为当前模型最大的架构缺陷是 query token 对 target 完全无感知。即使 MLP-1 已完成 (负向)，QRY-1 针对的是不同层面的问题。

### 7.6 反方审稿人反思 (方法论更新)

基于 YAO 的"反方审稿人"思维，我们对已有实验做回顾性质疑：

| 质疑 | 对象 | 分析 |
|------|------|------|
| **"为什么所有实验都负向？"** | Phase2/4/5 | 所有改动都增大了 Gap (Valid→Eval)，说明问题不是训练不够好，而是模型在验证集上过拟合。T2 之所以成功是因为 Cosine LR 让模型找到了更平坦的 loss landscape |
| **"Valid AUC 是不是在误导我们？"** | 所有实验 | T4b/T5/T6a/T9 的 Valid AUC 都 ≥ T2，但 Eval AUC 都 < T2。**Valid AUC 在我们的场景中是危险的信号**，它和 Eval AUC 几乎不相关 |
| **"微调方向对了吗？"** | Phase2-5 | 16 个实验仅 1 个正向 (T2)，说明在当前架构下微调已接近极限。**应转向架构级改动** |
| **"社区情报说 pure model 掉分，还能改模型吗？"** | YAO 洞察 | 社区情报说的 "pure model" 是指换 backbone (rankmixer/onetrans/hyformer/mixformer)。**在 HyFormer 框架内优化信息流（query/domain/gate）不算 "pure model change"**，而是 "架构优化" |
| **"SEQ1 的 -0.75% 说明了什么？"** | SEQ1-fid47 | fid47 不可移除，但社区说它"掉分"。矛盾说明：**不是要移除 ID 特征，而是要降低 ID 特征的过拟合权重**（GAP-3 仍然值得做）|

---

## 8. 优化方向三: 训练策略优化

### 8.1 已完成的训练策略实验

#### T-1: Sparse Embedding Re-init 策略 — 已完成

| 选项 | 实验 | Valid AUC | 结论 |
|------|------|-----------|------|
| 默认: epoch=1, thresh=0 | Phase0 | 0.8655 | ✅ 最佳(隐含在 baseline 中) |
| 关闭: epoch=-1 | T1a | CUDA OOM | ❌ 显存不足 |
| 选择性: epoch=2, thresh=100k | T1b | 0.8620 | ❌ Epoch 2 reinit 后 AUC 暴跌 |

**结论**: 默认的每 epoch 全量重置是最优策略，不需要改动

#### T-2: 学习率调度 — 已完成 ✅ 突破

| 选项 | 实验 | Valid AUC | Eval AUC | 结论 |
|------|------|-----------|----------|------|
| none (原始 v3) | v3 | 0.8636 | 0.8398 | baseline |
| cosine, warmup=500 | **T2** | **0.8658** | **0.8450** | ✅ **+0.52% 推理 AUC** |

**结论**: Cosine LR + Warmup 500 是目前最大突破，已纳入新 baseline

#### T-3: EMA 权重平均 — 已完成 ❌

| 选项 | 实验 | 状态 | 结论 |
|------|------|------|------|
| 无 EMA (当前 T2 baseline) | — | baseline | — |
| EMA, decay=0.999, warmup=100 | P2-T3 | ❌ KILLED | 训练极慢，不可用 |

**结论**: EMA 的 CPU-GPU shadow 同步导致训练速度严重下降（仅跑到 394 步），不适用于当前场景。v6 的负优化中 EMA 很可能是主因。

#### T-6a: Feature Masking — 已完成 ❌ 微弱负向

| 选项 | 实验 | Valid AUC | Eval AUC | 结论 |
|------|------|-----------|----------|------|
| 0 (当前) | T2 | 0.8658 | 0.8450 | baseline |
| 0.05 | T6a | 0.8660 | 0.8449 | ❌ 微弱负向 (-0.01%) |

**结论**: Feature Masking 0.05 使 Valid AUC 略高 (+0.02%) 但 Eval AUC 略低 (-0.01%)，说明增加了泛化 gap。不纳入 baseline。这也解释了 v6 中 FeatMask 的贡献为微弱负向。

### 8.2 进行中的训练策略实验

#### T-4b: Label Smoothing — 已完成 ❌

| 选项 | 实验 | Valid AUC | Eval AUC | 结论 |
|------|------|-----------|----------|------|
| 0 (当前) | — | 0.8658 | 0.8450 | baseline |
| 0.01 | P2-T4b | 0.8662 | **0.84164** | ❌ -0.34% |

**训练完整结果 (8 epochs)**:

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.8619 |
| 2 | 0.8638 |
| 3 | 0.8649 |
| 4 | 0.8651 |
| 5 | 0.8656 |
| **6** | **0.8662** |
| 7 | 0.8658 |
| 8 | 0.8660 |

**推理评估**: epoch 6 checkpoint (step 189228), evalTaskId 87536, Eval AUC = 0.84164

**结论**: Label Smoothing 0.01 使 Valid AUC 略高 (+0.04%) 但 Eval AUC 显著下降 (-0.34%)。Gap 从 2.08% 增至 2.46%。LS 没有改善泛化，反而增大了 Gap。与 v4 的 LS 0.05 负向经验一致。不纳入 baseline。

#### T-5: Dropout — 已完成 ❌

| 选项 | 实验 | Valid AUC | Eval AUC | 结论 |
|------|------|-----------|----------|------|
| 0.01 (当前) | — | 0.8658 | 0.8450 | baseline |
| 0.02 | P2-T5 | 0.8659 | **0.8429** | ❌ -0.21% |

**结论**: Dropout 从 0.01 提升到 0.02 是负向优化。Valid AUC 略高 (+0.01%) 但 Eval AUC 显著下降 (-0.21%)，Gap 从 2.08% 增至 2.30%。当前 dropout=0.01 已是最优。与 v4 经验一致（v4 的 dropout=0.05 也是负向）。

#### T-9: 训练轮数 — 已完成 ❌

| 选项 | 实验 | Valid AUC | Eval AUC | 结论 |
|------|------|-----------|----------|------|
| epochs=8, patience=3 (当前) | — | 0.8658 | 0.8450 | baseline |
| epochs=12, patience=5 | P2-T9 | ~0.8659 | **0.8428** | ❌ -0.21% |

**结论**: 更长训练 (12 epochs) 反而比 T2 的 8 epochs 差 (-0.21%)。使用的是 epoch 6 checkpoint (step 189228)，更多 epoch 导致过拟合。Cosine LR 按 12 epochs 计算总步数使衰减更慢，但更长的训练时间让模型在验证集上过拟合。T2 的 8 epochs + patience 3 已是最优配置。

### 8.3 待做的训练策略实验

| 实验 | 改动 | 选项 | 优先级 | 状态 |
|------|------|------|--------|------|
| T-5 | Dropout | 0.02 | P2 | ✅ 完成 (❌ 负向) |
| T-9 | 更长训练 | epochs=12 | P2 | ✅ 完成 (❌ 负向) |
| T-7 | Batch Size | 128 | P3 | 待做 |
| T-8 | 梯度累积 | 2, 4 | P4 | 待做 |

---

## 9. 优化方向四: 训练加速与工程优化

> **优先级: P1** — 不改变模型效果的前提下加速训练，为更多实验争取时间

### 9.1 训练加速方案

#### 背景

当前 T2 baseline 训练一轮（8 epochs, ~252K steps）耗时约 10-12 小时。EMA 实验因速度过慢被 KILLED。加速训练可以直接提升实验迭代速度。

#### 实验 ACCEL-1: torch.compile 加速

| 变量 | 值 |
|------|-----|
| 改动 | 使用 `torch.compile(model, mode="default")` 编译模型 |
| 预期 | 训练速度提升 10-30% |
| 风险 | 低 — 不改变模型结构，不影响训练结果 |
| 前提 | PyTorch 2.0+ 可用 |
| 状态 | v1 (reduce-overhead) ❌ Segfault; v2 (default) 🔄 训练中 |

**经验教训**:
- ❌ `mode="reduce-overhead"` 使用 CUDA Graphs，与模型的 sparse embedding 不兼容
- ✅ `mode="default"` 仅做算子融合，应该安全
- 需要 `torch.set_float32_matmul_precision('high')` 启用 TF32 加速

**实现要点**:
```python
# train.py 中，模型创建后
if args.use_compile:
    torch.set_float32_matmul_precision('high')
    model = torch.compile(model, mode="default")
```

#### 实验 ACCEL-2: BF16 混合精度训练

| 变量 | 值 |
|------|-----|
| 改动 | 使用 `torch.autocast(device_type="cuda", dtype=torch.bfloat16)` |
| 预期 | 训练速度提升 20-40%，显存减少 30-50% |
| 风险 | 中 — v5 使用 BF16 失败过，需排除其他变量干扰 |
| 前提 | GPU 支持 BF16 (Ampere+) |
| 状态 | v1 ❌ FAILED (TypeError); v2 🔄 训练中 (ep5, Eval 0.841327 负向) |

**控制变量设计**:
- **实验 A (BF16-v1)**: T2 baseline + BF16 only — ❌ FAILED (`TypeError: Got unsupported ScalarType BFloat16`)
- **实验 A (BF16-v2)**: T2 baseline + BF16 + `.float()` 修复 — 🔄 训练中, Eval 0.841327 **负向 -0.37%**
- **实验 B**: T2 baseline + torch.compile only — 🔄 v2 训练中 (ep2, Valid 0.8641)
- **实验 C**: T2 baseline + BF16 + torch.compile — 待 A/B 通过

> **与 v5 失败的区别**: v5 同时改了 AMP + LongerEncoder + RoPE + ffn_only + 大 batch，无法定位问题。这次**仅改精度**，其他完全不变。

**实现要点**:
```python
# trainer.py _train_step 中
if self.use_bf16:
    with torch.autocast(device_type="cuda", dtype=torch.bfloat16):
        logits = self.model(inputs)
        loss = self._compute_loss(logits, labels)
```
- BF16 不需要 GradScaler（与 FP16 不同），更简单
- 添加 `--use_bf16` flag（默认关闭）
- 验证时也需要 autocast 或转为 FP32

#### 实验 ACCEL-3: Gradient Checkpointing

| 变量 | 值 |
|------|-----|
| 改动 | 对 HyFormer blocks 启用 gradient checkpointing |
| 预期 | 显存减少 50-70%，可增大 batch_size 或加深模型 |
| 风险 | 低 — 不改变训练结果，仅影响显存/速度权衡 |
| 代价 | 训练速度降低 ~20%（重计算换显存） |

**适用场景**: 当需要增大 batch_size 或增加模型深度时，用 gradient checkpointing 释放显存

### 9.2 加速实验优先级

| 优先级 | 实验 | 改动 | 预期加速 | 风险 | 状态 |
|--------|------|------|---------|------|------|
| P1 | ACCEL-1v2 | torch.compile (default) | 10-30% | 低 | 🔄 训练中 (ep2, Valid 0.8641) |
| P1 | ACCEL-2A-v1 | BF16 only | 20-40% | 中 | ❌ FAILED (TypeError BFloat16) |
| P1 | ACCEL-2A-v2 | BF16 only (修复后) | 20-40% | 中 | ❌ 完成 (ep5 Eval 0.8433, 负向 -0.17%) |
| P2 | ACCEL-2C | BF16 + compile | 30-50% | 中 | 待 ACCEL-1 通过 |
| P3 | ACCEL-3 | Gradient Checkpointing | 显存-50% | 低 | 待做 |

> **关键发现**: BF16-v2 完整评估显示 ep4 Eval 0.8413 / ep5 Eval 0.8433，均低于 T2 的 0.8450。BF16 混合精度**不可用于竞赛提交**。可能仅用于加速实验迭代。

> **关键约束**: 每个 ACCEL 实验都必须与 T2 baseline 做**控制变量对比**，确认 AUC 不掉分后再使用。

---

## 10. 优化方向五: 验证协议修正

> **状态: 已完成** ✅ (Phase0)

### 10.1 Phase0 已完成

| 项目 | 结果 |
|------|------|
| 时间切分验证 | ✅ 已实现, 按 timestamp 排序后取最新数据作为验证集 |
| 确定性种子 | ✅ 已实现, torch deterministic + 固定 IterableDataset seed |
| Phase0 Valid AUC | 0.8655 (8 epoch, Best at Epoch 6) |

**关键发现**: 时间切分后 Valid AUC (0.8655) 反而略高于原始 v3 (0.8636)，说明之前的验证集划分并没有过于乐观。

---

## 11. 优化方向六: 后处理与模型导出

> **优先级: P2** — 在训练策略+特征优化完成后做

| 方案 | 方法 | 预期 |
|------|------|------|
| SWA | 训练最后 20% epoch 做权重平均 | AUC +0.0~0.2% |
| Temperature Scaling | 在验证集上学习温度参数 T | LogLoss 改善，AUC 不变 |

---

## 12. 优化方向七: 缩小 Valid→Eval AUC Gap

> **优先级: P1** — 当前 Valid AUC ~0.8658 但推理 Eval AUC 仅 0.8450，Gap 达 **2.08%**
> **目标**: 在不降低 Valid AUC 的前提下提升 Eval AUC，或在保持 Eval AUC 的前提下降低 Gap

### 12.1 Gap 分析

| 实验 | Best Valid AUC | Eval AUC | Gap | 分析 |
|------|---------------|----------|-----|------|
| Phase0 | 0.8655 | 0.8387 | 2.68% | 基线 Gap，时间切分验证 |
| **T2** | **0.8658** | **0.8450** | **2.08%** | Cosine LR 缩小了 Gap (-0.60%) |
| T6a | 0.8660 | 0.8449 | 2.11% | FeatMask 略增大 Gap |
| T5 | 0.8659 | 0.8429 | **2.30%** | Dropout 增大 Gap (+0.22%) |
| BF16-v2 (ep5) | 0.8663 | 0.84327 | **2.30%** | BF16 精度损失增大 Gap |
| T9 | ~0.8659 | 0.8428 | **~2.31%** | 更长训练增大 Gap (+0.23%) |
| SEQ2a (ep4) | 0.8665 | 0.842944 | **-2.39%** | 时间周期特征负向，Gap 从 T2 的 2.08% 增至 -2.39% (异常，过拟合) |
| SEQ2a (ep7) | 0.8681 | 0.84009 | **-2.80%** | 时间周期特征负向，Gap 进一步增大，严重过拟合 |
| SEQ2a (ep8) | 0.8683 | 0.838095 | **-2.92%** | 时间周期特征负向，Gap 最大，极度过拟合 |
| T4b | 0.8662 | 0.84164 | **2.46%** | Label Smoothing 增大 Gap (+0.38%) |
| BF16-v2 (ep4) | 0.8663 | 0.841327 | **~2.50%** | BF16 精度损失增大 Gap |
| v3 | 0.8636 | 0.8398 | 2.38% | 原始基线 |
| SEQ1-fid47Treatment | - | 0.8375 | - | 移除 seq_c fid47 严重负向 (-0.75%) |

> *BF16-v2 Best Valid AUC at epoch 5, Eval using epoch 4 checkpoint

**关键洞察**:
1. **Cosine LR 是唯一成功缩小 Gap 的改动** — 从 2.68% 降到 2.08%，说明学习率调度对泛化至关重要
2. **Feature Masking 增大了 Gap** — Valid 略高但 Eval 略低，说明正则化方式不当可能增加过拟合
3. **Dropout 增大也增大了 Gap** — T5 的 Gap 从 2.08% 增至 2.30%，更强的 dropout 没有改善泛化
4. **更长训练增大了 Gap** — T9 的 Gap ~2.31%，12 epochs 过拟合验证集
5. **Label Smoothing 增大了 Gap** — T4b 的 Gap 2.46%，LS 0.01 同样负向
6. **BF16 精度损失增大了 Gap** — BF16-v2 的 Gap ~2.50%，精度损失不可逆
7. **规律**: 所有超出 T2 的改动（Dropout、FeatMask、更长训练、LS、BF16、移除 fid47）都导致 Valid AUC 略高但 Eval AUC 更低，Gap 增大
8. **Gap 的三个来源**:
   - **验证集偏差**: 验证集来自训练集尾部 5% 时间窗口，与真实测试集分布不完全一致
   - **特征过拟合**: ID 类特征 (fid47 等) 在训练集上虚高但无法泛化
   - **时间漂移**: 测试集时间可能比训练集更新，存在分布偏移

### 12.2 Gap 缩小方案

#### GAP-1: 验证集策略优化 (不改变训练)

| 变量 | 值 |
|------|-----|
| 改动 | 增大验证集比例或改变选择策略 |
| 方案 A | `valid_ratio` 从 0.05 → 0.10 (验证集翻倍) |
| 方案 B | 在验证集上做 early stopping 时，选择 LogLoss 最低而非 AUC 最高的 checkpoint |
| 预期 | 更可靠的 checkpoint 选择，可能选出泛化更好的模型 |
| 风险 | 低 — 不改变训练过程 |

**注意**: GAP-1 不直接提升 Eval AUC，但可能选出更好的 checkpoint。

#### GAP-2: 已完成的正则化实验

| 实验 | 改动 | Gap 变化 | 状态 |
|------|------|---------|------|
| T5-Dropout | Dropout 0.02 | Gap **增大** (2.08%→2.30%) | ❌ 负向 |
| T6a-FeatMask | FeatMask 0.05 | Gap **增大** (2.08%→2.11%) | ❌ 微弱负向 |
| T9-Long | 更多 epoch (12) | Gap **增大** (2.08%→~2.31%) | ❌ 负向 |
| T4b-LS | Label Smoothing 0.01 | Gap **增大** (2.08%→2.46%) | ❌ 负向 |

**重要发现**: 更强的正则化 (Dropout/FeatMask/LS) 和更长训练都反而增大了 Gap，说明在 T2 基础上继续调优训练策略已无空间。Phase2 全部 5 个实验均验证负向。

#### GAP-3: ID 特征降权/去除

| 变量 | 值 |
|------|-----|
| 改动 | 识别并处理导致过拟合的 ID 类特征 |
| 方案 A | 将 ID 特征的 `emb_skip_threshold` 设为 0 (跳过) |
| 方案 B | 对 ID 特征使用 hash bucket 替代直接 embedding |
| 方案 C | 对 ID 特征降低 lr 或加强 weight_decay |
| 预期 | 直接减少特征过拟合，缩小 Gap |
| 风险 | 中 — 需要先识别哪些特征是 ID 类 |

**社区情报**: "ID 特征线下虚高但线上掉分"，这与我们的 Gap 分析一致。这是**高风险高收益**的优化点。

#### GAP-4: SWA (Stochastic Weight Averaging)

| 变量 | 值 |
|------|-----|
| 改动 | 在训练最后 20-30% epoch 做权重平均 |
| 方法 | 每 N 步 snapshot 一次参数，最后取平均 |
| 预期 | 平滑损失函数平面，改善泛化 (AUC +0.0~0.2%) |
| 风险 | 低 — 纯后处理，不改变训练过程 |
| 优势 | 不像 EMA 需要每步同步，SWA 只需 snapshot + 平均 |

**与 EMA 的区别**: EMA 每步更新 shadow 权重 (CPU↔GPU 同步开销大)，SWA 只在验证后 snapshot，开销极低。T3 (EMA) 因训练太慢被 KILLED，但 SWA 无此问题。

#### GAP-5: Early Stopping 策略优化

| 变量 | 值 |
|------|-----|
| 改动 | 改变 early stopping 的指标或策略 |
| 方案 A | 基于 LogLoss 而非 AUC 做 early stopping |
| 方案 B | patience 从 3 → 2 (更早停止，防止过拟合) |
| 方案 C | 模型保存时保存最近 3 个 epoch，推理时选最优 |
| 预期 | 防止在验证集上过拟合导致 Gap 增大 |
| 风险 | 低 |

### 12.3 Gap 缩小实验优先级

| 优先级 | 实验编号 | 实验名称 | 预期 Gap 缩小 | 风险 | 实现难度 |
|--------|---------|---------|--------------|------|---------|
| P0 | T4b | (已完成) LS 0.01 | ❌ Gap 增大 | 低 | ❌ 负向 |
| P1 | GAP-4 | SWA 权重平均 | 0.05~0.20% | 低 | 低 |
| P1 | GAP-3 | ID 特征降权 | 0.10~0.30% | 中 | 中 |
| P2 | GAP-5 | Early Stopping 优化 | 0.00~0.10% | 低 | 低 |
| P2 | GAP-1 | 验证集策略 | 间接改善 | 低 | 低 |

### 12.4 Gap 分析与模型改进的协同

```
                     Valid AUC 高但 Eval AUC 低 = Gap 大
                              │
                    ┌─────────┴─────────┐
                    │                   │
              过拟合训练集           验证集偏差
              (Gap = 2.08%)        (Valid 不代表 Eval)
                    │                   │
            ┌───────┴───────┐    ┌───────┴───────┐
            │               │    │               │
       正则化不够      特征过拟合   验证集太小    验证集分布
       (Dropout/LS)   (ID 特征)   (ratio 5%)   与测试集不同
            │               │    │               │
         T5/T4b          GAP-3   GAP-1         GAP-1/5
```

**核心策略**: T5/T6a/T9 均已确认负向，Gap 缩小应从 GAP-4 (SWA，低风险) 和 GAP-3 (ID 特征，高收益) 入手，而非继续调训练超参。

---

## 13. UniRec 参考项目分析

> **来源**: https://github.com/hojiahao/TAAC2026 (UniRec 项目)
> **评估日期**: 2026-05-12

### 12.1 项目概述

UniRec 是另一个 TAAC2026 参赛选手的开源方案，提供了完整的训练+推理流水线。以下逐项分析其与我们方案的差异和可借鉴之处。

### 13.2 可借鉴技术清单

#### A. 训练工程优化 (可直接应用)

| 技术 | UniRec 实现 | 我们的现状 | 可行性 |
|------|------------|-----------|--------|
| **BF16 混合精度** | `torch.autocast(dtype=bfloat16)`，无需 GradScaler | FP32 训练 | ⚠️ 中 — BF16-v2 Eval AUC 下降 0.37%，不可用于竞赛提交，可能仅用于加速迭代 |
| **torch.compile** | `torch.compile(mode="reduce-overhead")` | 未使用 | ✅ 高 — 见 ACCEL-1 |
| **Gradient Checkpointing** | `model.gradient_checkpointing_enable()` | 未使用 | ✅ 高 — 见 ACCEL-3 |
| **DDP 双卡训练** | `DistributedDataParallel` 2 卡 | 单卡 | ⚠️ 中 — 太极平台限制不确定 |

#### B. 优化器与损失函数 (需独立验证)

| 技术 | UniRec 实现 | 我们的现状 | 风险评估 |
|------|------------|-----------|---------|
| **Muon + AdamW 混合优化器** | Muon 用于注意力层，AdamW 用于其他层 | 全部 AdamW | ⚠️ 中 — Muon 是新优化器，需要单独验证 |
| **CombinedAUCLoss** | `(1-α)·WeightedBCE + α·PairwiseBPR` | 纯 BCE | ⚠️ 中 — v4 的 Pairwise 已证明负向，但权重混合可能不同 |
| **AUC-aware Loss** | 直接优化 AUC 相关指标 | BCE (间接优化 AUC) | ⚠️ 中 — 需要控制变量验证 |

**评估**: Muon 优化器是 2024 年的新方法（由 Jordan et al. 提出），理论上有更好的收敛性。但 Pairwise Loss 在我们的 v4 中已证明负向，CombinedAUCLoss 的 Pairwise 部分需谨慎。

#### C. 模型结构创新 (高风险，谨慎借鉴)

| 技术 | UniRec 实现 | 我们的现状 | 风险评估 |
|------|------------|-----------|---------|
| **Feature Cross Layer** | 来自 InterFormer，显式特征交叉 | 有 DCN-V2 Cross Fusion | ⚠️ 中 — 功能类似但实现不同 |
| **SiLU Gated Attention** | 来自 HSTU 2.0，替代标准 softmax attention | 标准 softmax + SiLU activation | ❌ 高 — 改动注意力核心，社区情报表明纯模型改动掉分 |
| **Block Attention Residuals** | 来自 Kimi，每层有残差连接 | HyFormer 内部已有残差 | ⚠️ 中 — 增量改动 |
| **Semi-Local Attention** | 局部+全局注意力混合 | 标准 Transformer 全序列 | ❌ 高 — 架构改动大 |

#### D. 数据处理 (可直接参考)

| 技术 | UniRec 实现 | 我们的现状 | 可行性 |
|------|------------|-----------|--------|
| **时间特征处理** | 提取 hour/day 等周期特征 | 仅 time-delta bucket | ✅ 高 — 见 SEQ-2a |
| **特征归一化** | 对 dense 特征做标准化 | 无归一化 | ✅ 高 — 见 U-D3 |
| **负采样策略** | 特定的负采样比例 | 隐式（BCE over 全量） | ⚠️ 低 — 当前数据流不支持 |

### 13.3 综合评估: 可直接整合的优化

按优先级排序，从 UniRec 借鉴并整合到我们的控制变量实验框架中：

| 优先级 | 技术 | 对应我们的实验 | 预期影响 | 实施建议 |
|--------|------|--------------|---------|---------|
| **P0** | BF16 混合精度 | ACCEL-2 | 加速 20-40% | ❌ Eval AUC 下降 0.37%，不可用于正式提交，可考虑仅用于迭代加速 |
| **P0** | torch.compile | ACCEL-1 | 加速 10-30% | 立即实验，风险极低 |
| **P1** | 时间周期特征 | SEQ-2a | AUC +0.1~0.3% | 从 UniRec 参考实现，适配到我们的 dataset.py |
| **P2** | Muon 优化器 | 新实验 OPT-1 | 待验证 | 作为独立控制变量实验 |
| **P2** | Dense 特征归一化 | U-D3 | AUC +0.0~0.1% | 简单实现，低风险 |
| **P3** | Feature Cross Layer | 新实验 FC-1 | 待验证 | 与现有 Cross Fusion 对比 |
| **P4** | CombinedAUCLoss | 新实验 LOSS-1 | 高风险 | v4 Pairwise 已负向，谨慎 |

### 13.4 不建议借鉴的技术

| 技术 | 原因 |
|------|------|
| SiLU Gated Attention | 架构改动大，社区情报表明纯模型改动掉分 |
| Semi-Local Attention | 架构改动大，风险高 |
| DDP 双卡训练 | 太极平台限制不确定，且需要代码大改 |
| Block Attention Residuals | 增量收益不确定，需更多调研 |

### 13.5 新增实验列表 (来自 UniRec 参考)

| 实验 | 改动 | 基线 | 说明 |
|------|------|------|------|
| OPT-1 | Muon + AdamW 混合优化器 | T2 | 需要安装 Muon 库，仅优化器改变 |
| LOSS-1 | CombinedAUCLoss (α=0.1) | T2 | BCE + Pairwise 混合，v4 的 Pairwise 是纯 Pairwise |
| FC-1 | Feature Cross Layer | T2 | 替换或补充现有 Cross Fusion |

---

## 14. 实验执行路线图

### 14.1 分阶段执行计划 (已更新)

```
Phase 0: 基础设施 ✅ 已完成
├── V-1: 时间切分验证          → Valid AUC 0.8655
└── V-2: 随机种子固定          → 确定性可复现

Phase 1: 高收益低风险训练策略 ✅ 已完成
├── T-1a: 关闭 re-init         → CUDA OOM 失败
├── T-1b: 选择性 re-init       → Valid 0.8620 负向 (早停)
└── T-2: Cosine LR             → Eval AUC 0.8450 ★ 突破 +0.52%

Phase 2: 基于 T2 的进一步消融 ✅ 全部完成 (均为负向)
├── P2-T3: EMA 0.999           → ❌ KILLED (训练极慢)
├── P2-T4b: Label Smoothing    → ❌ 完成 (Eval 0.8416, 负向 -0.34%, Gap 2.46%)
├── P2-T5: Dropout 0.02        → ❌ 完成 (Eval 0.8429, 负向 -0.21%)
├── P2-T6a: FeatMask 0.05      → ❌ 完成 (Eval 0.8449, 微弱负向)
└── P2-T9: 更长训练            → ❌ KILLED (Eval 0.8428, 负向 -0.21%)

Phase 3: 训练加速 (并行进行) 🔄
├── ACCEL-1: torch.compile v1 → ❌ Segfault (CUDA Graphs 不兼容)
├── ACCEL-1v2: torch.compile v2 → 🔄 mode=default, 训练中 (ep2, Valid 0.8641)
├── ACCEL-BF16-v1             → ❌ FAILED (TypeError: BFloat16 .numpy())
└── ACCEL-BF16-v2             → ❌ 完成 (ep5 Eval 0.8433, 负向 -0.17%)

Phase 3.5: Gap 缩小 🆕
├── GAP-4: SWA 权重平均             🆕 低风险，不等 EMA
├── GAP-3: ID 特征降权/去除         🆕 高收益
└── GAP-5: Early Stopping 优化      🆕

Phase 4: 数据与特征工程 (已启动) 🔄
├── SEQ-2a: 全局时间周期 (hour+day) → ❌ ep4 Eval 0.8429, ep7 Eval 0.8401, ep8 Eval 0.8381, 全负向
├── U-D1: User Dense MLP (918→128→64) → 🔄 v2 训练中 (v1 OOM at ep2, ep4 Valid 0.8642, 待评估)
├── SEQ-1: seq_c fid47 处理        → ❌ ep4 Eval 0.8375, 严重负向 -0.75% (fid47 不可移除)
├── U-D2: Shared-FID 联合建模
└── IT-1: ID 特征处理

Phase 5: 模型微调 (谨慎) 🔄
├── MLP-1: Classifier 加深           → ❌ Eval 0.840612, -0.43% vs T2, 负向
├── M-2: HyFormer Blocks 1         → ⚠️ 训练完成, Valid 0.8654 (ep7), 权重未保存无 Eval, -0.04% vs T2
├── M-3: HyFormer Blocks 3         → ⚠️ 训练完成, Valid 0.8655 (ep4), 权重未保存无 Eval, -0.03% vs T2
└── DIN-1: 前端 DIN Query 增强       → 待做

Phase 5.5: YAO 洞察驱动架构优化 ❌ 全部负向
├── QRY-1: Target-Aware Query Interface → ❌ Eval 0.844115, -0.08%
├── DOM-1: 域级动态可靠性门控          → ❌ Eval 0.843100, -0.19%
└── TEMP-1: 时间条件化特征门控         → ❌ Eval 0.843035, -0.19%

Phase 6: LR 策略深度搜索 🆕 P0 (最有可能突破)
├── LR-1: Warmup 调优 (300/700/1000)
├── LR-2: min_lr_ratio 调优 (0.05/0.2)
├── LR-3: Polynomial Decay
└── LR-4: OneCycleLR

Phase 7: ID 特征降权 🆕 P1
├── ID-1: Hash Bucket
├── ID-2: 学习率降权
└── ID-3: Weight Decay

Phase 8: Checkpoint 策略 🆕 P1
├── CKPT-1: 保存所有 epoch
├── CKPT-2: LogLoss early stopping
└── CKPT-3: Patience=2

Phase 9: 高级优化 (来自 UniRec 参考)
├── OPT-1: Muon 优化器
├── LOSS-1: CombinedAUCLoss
└── FC-1: Feature Cross Layer

Phase 10: 收尾
├── 组合所有已验证有效的改动
├── 最终超参精调
└── 提交最终版本
```

### 14.2 实验决策树 (已更新)

```
                    V3 Baseline (0.8398)
                         │
                    Phase 0: 验证修正 ✅
                    Valid AUC = 0.8655
                         │
                    Phase 1: 训练策略 ✅
                    ┌────┴────┬────────┐
               T-1a: OOM  T-1b: 负向  T-2: 突破
                                   │
                              T2 Baseline: Eval AUC = 0.8450
                                   │
                    Phase 2: 基于 T2 消融 ✅ 全部完成
                    ┌───┴───┬─────────┬────────┬────────┐
                  T3:EMA  T4b:LS   T5:Dropout T6a:FM  T9:Long
                  KILLED   完成❌    完成❌     完成❌   KILLED
                   ❌     0.8416   0.8429    0.8449   0.8428
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
              Phase 3: 训练加速 🔄          Phase 3.5: Gap 缩小 🆕
              ACCEL-1v2: compile(default)    GAP-4: SWA
              ACCEL-BF16-v2: BF16 (❌ 负向)  GAP-3: ID 特征降权
              (BF16 不改变 AUC ❌)           GAP-5: Early Stop 优化
                    │                             │
                    └──────────────┬──────────────┘
                                   │
                    Phase 4: 特征工程 🔄
                    ├── SEQ-2a: 时间周期 (❌ ep4/7/8 全负向, ep4=0.8429, ep7=0.8401, ep8=0.8381)
                    ├── U-D1: Dense MLP (🔄 v2 训练中, ep4 Valid 0.8642)
                    ├── SEQ-1: fid47 (❌ ep4 Eval 0.8375, 严重负向)
                    └── U-D2: Dense 分层
                                   │
                    Phase 5: 模型微调 (谨慎) 🔄
                    ├── MLP-1: Classifier 加深 (❌ Eval 0.8406, -0.43%)
                    ├── M-2: HyFormer Blocks 1 (⚠️ Valid 0.8654, -0.04%, 权重未保存无 Eval)
                    ├── M-3: HyFormer Blocks 3 (⚠️ Valid 0.8655, -0.03%, 权重未保存无 Eval)
                    └── DIN-1: 前端 target 感知
                                   │
                    Phase 5.5: YAO 架构优化 🆕
                    ├── QRY-1: Target-Aware Query (🆕 P0, query token 融合 target)
                    ├── DOM-1: 域级动态门控 (🆕 P1, 动态加权行为域)
                    └── TEMP-1: 时间条件化门控 (🆕 P2, temporal drift)
                                   │
                    Phase 6: 高级优化 (UniRec 参考)
                    ├── OPT-1: Muon 优化器
                    └── LOSS-1: CombinedAUC
                                   │
                    Phase 7: 收尾 (组合 + 精调)
```

---

## 15. 实验记录

### Phase0: 时间切分 + 种子固定

**日期**: 2026-05-09
**基于版本**: v3 (第三次-HyFormer)
**改动变量**: +time_split +deterministic_seed

#### 改动配置

| 参数 | v3 值 | Phase0 值 |
|------|-------|----------|
| time_split | False | True |
| seed | 默认 | 42 (deterministic) |

#### 训练结果

| Epoch | Valid AUC | LogLoss |
|-------|-----------|---------|
| 1 | 0.862041 | 0.230166 |
| 2 | 0.863812 | 0.228709 |
| 3 | 0.864331 | 0.226621 |
| 4 | 0.864560 | 0.226831 |
| 5 | 0.864911 | 0.226437 |
| **6** | **0.865510** | **0.226389** |
| 7 | 0.864144 | 0.227132 |
| 8 | 0.864944 | 0.226035 |

#### 推理评估

| 指标 | Phase0 |
|------|--------|
| 推理 Eval AUC | 0.838662 |

#### 结论
- ✅ 基础设施完善 — 作为后续实验的可靠基线
- Phase0 配置 + T2 的默认 reinit 策略 (epoch=1, thresh=0) 已是最优

---

### Phase1-T1a: 关闭 Sparse Re-init

**日期**: 2026-05-09
**基于版本**: Phase0 baseline
**改动变量**: reinit_sparse_after_epoch=-1 (关闭)

#### 结论
- ❌ 训练失败 — CUDA OOM
- 关闭 re-init 导致 embedding 参数常驻 GPU，显存不足

---

### Phase1-T1b: 选择性 Sparse Re-init

**日期**: 2026-05-09
**基于版本**: Phase0 baseline
**改动变量**: reinit_sparse_after_epoch=2, reinit_cardinality_threshold=100000

#### 训练结果

| Epoch | Valid AUC | LogLoss | 备注 |
|-------|-----------|---------|------|
| 1 | 0.862044 | 0.230164 | |
| 2 | 0.848403 | 0.237629 | ⚠️ reinit 后暴跌 |
| 3 | 0.861945 | 0.229616 | 部分恢复 |
| 4 | 0.860210 | 0.231086 | 未恢复 |

#### 结论
- ❌ 负向优化 — Epoch 2 reinit 后 AUC 暴跌至 0.848
- Best Valid AUC 0.8620 远低于 Phase0 的 0.8655
- 选择性 reinit 比默认全量重置效果差

---

### Phase1-T2: Cosine LR Scheduler ★ 突破

**日期**: 2026-05-09
**基于版本**: Phase0 baseline
**改动变量**: lr_scheduler=cosine, warmup_steps=500, min_lr_ratio=0.1

#### 训练结果

| Epoch | Valid AUC | LogLoss |
|-------|-----------|---------|
| 1 | 0.861879 | 0.229747 |
| 2 | 0.863936 | 0.228208 |
| 3 | 0.864834 | 0.225909 |
| 4 | 0.865036 | 0.225680 |
| 5 | 0.865066 | 0.225545 |
| **6** | **0.865796** | **0.224631** |
| 7 | 0.865222 | 0.224743 |
| 8 | 0.865385 | 0.224443 |

#### 推理评估

| 指标 | Phase0 (baseline) | T2 | 差异 |
|------|-------------------|-----|------|
| 训练 Best Valid AUC | 0.8655 | 0.8658 | +0.0003 |
| **推理 Eval AUC** | 0.8387 | **0.8450** | **+0.63%** |
| 对比 v3 (0.8398) | -0.11% | **+0.52%** | — |
| Gap (Valid→Eval) | 2.68% | 2.08% | -0.60% |

#### 结论
- ✅ **重大突破** — 推理 AUC 从 0.8398 提升到 0.8450 (+0.52%)
- Valid AUC 基本持平但推理 AUC 显著提升，说明 Cosine LR 改善了泛化
- Gap 从 2.38% 缩小到 2.08%
- **已纳入新 baseline**

---

### Phase2-T3: EMA 0.999

**日期**: 2026-05-11
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_ema, ema_decay=0.999, ema_warmup_steps=100

#### 状态: ❌ KILLED

**分析**: 仅跑到 394 个 training step，只记录了 Loss/train，无 Valid AUC 数据。EMA 在每步训练后需要将 GPU 参数同步到 CPU shadow 并更新，导致训练速度严重下降。在太极平台有限的训练时间内无法完成。

#### 结论
- ❌ EMA 不可用于当前场景
- CPU-GPU 数据搬运开销导致训练速度严重下降
- 这很可能是 v6 负优化的主因之一

---

### Phase2-T4b: Label Smoothing 0.01

**日期**: 2026-05-11 ~ 2026-05-13
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +label_smoothing=0.01

#### 状态: ✅ 已完成 (❌ 负向)

#### 训练结果 (8 epochs, 训练成功完成)

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.8619 |
| 2 | 0.8638 |
| 3 | 0.8649 |
| 4 | 0.8651 |
| 5 | 0.8656 |
| **6** | **0.8662** |
| 7 | 0.8658 |
| 8 | 0.8660 |

#### 推理评估

| 指标 | T2 (baseline) | T4b | 差异 |
|------|---------------|-----|------|
| Best Valid AUC | 0.8658 | **0.8662** | +0.04% |
| **Eval AUC** | **0.8450** | 0.84164 | **-0.34%** |
| Gap (Valid→Eval) | 2.08% | **2.46%** | +0.38% |

> epoch 6 checkpoint (step 189228), evalTaskId 87536

#### 结论
- ❌ **负向** — Valid AUC 略高 (+0.04%) 但 Eval AUC 显著下降 (-0.34%)
- Label Smoothing 0.01 增大了泛化 Gap（从 2.08% 增至 2.46%）
- 与 v4 的 LS 0.05 负向经验一致，Label Smoothing 整体不可用
- **Phase2 最后一个实验确认负向，Phase2 全部 5 个实验均为负向**
- 不纳入 baseline

---

### Phase2-T5: Dropout 0.02

**日期**: 2026-05-11
**基于版本**: T2 baseline (cosine LR)
**改动变量**: dropout_rate=0.02 (从 0.01 提升)

#### 状态: ✅ 已完成

#### 训练结果

| Step | Valid AUC | LogLoss |
|------|-----------|---------|
| 31538 | 0.861576 | 0.230399 |
| 63076 | 0.864254 | 0.227895 |
| 94614 | 0.865242 | 0.225576 |
| 126152 | 0.865411 | 0.225229 |
| 157690 | 0.865413 | 0.224827 |
| **189228** | **0.865870** | **0.224569** |

#### 推理评估

| 指标 | T2 (baseline) | T5 | 差异 |
|------|---------------|-----|------|
| Best Valid AUC | 0.8658 | **0.8659** | +0.01% |
| **Eval AUC** | **0.8450** | **0.842873** | **-0.21%** |
| Gap (Valid→Eval) | 2.08% | **2.46%** | +0.22% |

> **评估结论**:
> - ❌ **负向** — Valid AUC 略高 (+0.01%) 但 Eval AUC 显著下降 (-0.21%)
> - Dropout 从 0.01 提升到 0.02 反而增大了泛化 Gap（从 2.08% 增至 2.30%）
> - 更强的 dropout 正则化没有改善泛化，反而降低了推理表现
> - **与 T6a 经验一致**: v4 中 dropout=0.05 也是负向。当前 dropout=0.01 已是最优
> - 不纳入 baseline
> - **Eval Task ID**: 85543

#### 结论
- ❌ **负向** — Valid AUC 略高 (+0.01%) 但 Eval AUC 显著下降 (-0.21%)
- Dropout 从 0.01 提升到 0.02 反而增大了泛化 Gap（从 2.08% 增至 2.30%）
- 更强的 dropout 正则化没有改善泛化，反而降低了推理表现
- **与 v4 经验一致**: v4 中 dropout=0.05 也是负向。当前 dropout=0.01 已是最优
- 不纳入 baseline

---

### Phase2-T6a: Feature Masking 0.05

**日期**: 2026-05-11
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +feature_mask_ratio=0.05

#### 状态: ✅ 已完成

#### 训练结果

| Epoch | Step | Valid AUC | LogLoss |
|-------|------|-----------|---------|
| 1 | 31538 | 0.861757 | 0.229945 |
| 2 | 63076 | 0.864027 | 0.228060 |
| 3 | 94614 | 0.864684 | 0.225892 |
| 4 | 126152 | 0.864887 | 0.225335 |
| 5 | 157690 | 0.865815 | 0.224524 |
| **6** | **189228** | **0.865978** | **0.224387** |
| 7 | 220766 | 0.865466 | 0.224382 |
| 8 | 252304 | 0.865832 | 0.224210 |

#### 推理评估

| Checkpoint | Eval AUC | 对比 T2 |
|-----------|----------|---------|
| epoch 4 (step 126152) | **0.844911** | -0.004% |
| epoch 5 (step 157690) | 0.844416 | -0.054% |

#### 对比 T2

| 指标 | T2 (baseline) | T6a | 差异 |
|------|---------------|-----|------|
| Best Valid AUC | 0.8658 | **0.8660** | +0.02% |
| Best Eval AUC | **0.8450** | 0.8449 | -0.01% |
| Gap | 2.08% | 2.11% | +0.03% |

#### 结论
- ❌ **微弱负向** — Valid AUC 略高 (+0.02%) 但 Eval AUC 略低 (-0.01%)
- Feature Masking 增加了泛化 gap（Valid→Eval 多损失 0.03%）
- 不纳入 baseline

---

### Phase2-T9: 更长训练

**日期**: 2026-05-11
**基于版本**: T2 baseline (cosine LR)
**改动变量**: num_epochs=12 (原 8), patience=5 (原 3)

#### 状态: ❌ KILLED (已完成推理评估)

#### 训练结果

| Step | Valid AUC | LogLoss |
|------|-----------|---------|
| 31538 | 0.861851 | 0.230126 |
| 63076 | 0.863815 | 0.228369 |
| 94614 | 0.864327 | 0.226667 |
| 126152 | 0.864936 | 0.226462 |
| 157690 | — | — |
| **189228** | **~0.8659** | — |

> 使用 epoch 6 checkpoint (step 189228) 进行推理评估

#### 推理评估

| 指标 | T2 (baseline) | T9 | 差异 |
|------|---------------|-----|------|
| Best Valid AUC | 0.8658 | ~0.8659 | +0.01% |
| **Eval AUC** | **0.8450** | 0.8428 | **-0.21%** |
| Gap (Valid→Eval) | 2.08% | **~2.31%** | +0.23% |

> 首次推理 (evalTaskId 86771) 因 CUDA OOM 失败，修复 batch_size 后成功 (evalTaskId 86792)

#### 结论
- ❌ **负向** — Valid AUC 略高但 Eval AUC 显著下降 (-0.21%)
- 更长训练 (12 epochs) 在 Cosine LR 下反而更差，说明 8 epochs 已是最优
- 更多 epoch 让模型在验证集上过拟合，Gap 从 2.08% 增至 ~2.31%
- **与 T5 (Dropout 0.02) 一致**: 更强的训练策略都增大了 Gap
- 不纳入 baseline

---

### 消融实验汇总表

| 编号 | 实验名称 | 改动 | Best Valid AUC | Eval AUC | 对比 T2 | 结论 |
|------|---------|------|---------------|----------|---------|------|
| Phase0 | 时间切分+种子 | +time_split +seed | 0.8655 | 0.8387 | -0.63% | ✅ 基线建立 |
| T1a | 关闭 re-init | epoch=-1 | — | — | — | ❌ OOM |
| T1b | 选择性 re-init | epoch=2, thresh=100k | 0.8620 | 待评估 | — | ❌ 负向 |
| **T2** | **Cosine LR** | cosine, warmup=500 | **0.8658** | **0.8450** | **baseline** | ✅ **最佳** |
| P2-T3 | EMA 0.999 | T2 + EMA | — | — | — | ❌ KILLED |
| P2-T4b | LS 0.01 | T2 + LS(0.01) | **0.8662** | 0.84164 | -0.34% | ❌ 负向 |
| P2-T5 | Dropout 0.02 | T2 + dropout=0.02 | 0.8659 | 0.8429 | -0.21% | ❌ 负向 |
| P2-T6a | FeatMask 0.05 | T2 + featmask | 0.8660 | 0.8449 | -0.01% | ❌ 微弱负向 |
| P2-T9 | 更长训练 | T2 + epochs=12 | ~0.8659 | 0.8428 | -0.21% | ❌ KILLED |
| P3-ACCEL-v1 | torch.compile | T2 + compile(reduce-overhead) | — | — | — | ❌ Segfault |
| P3-ACCEL-v2 | torch.compile | T2 + compile(default) | 待获取 | 待获取 | ? | 🔄 训练中 |
| P3-ACCEL-BF16-v1 | BF16 混合精度 | T2 + use_bf16 | — | — | — | ❌ FAILED (TypeError) |
| P3-ACCEL-BF16-v2 | BF16 混合精度 | T2 + use_bf16 (修复) | 0.8663 | 0.84327 | -0.17% | ❌ 负向 (ep4=0.8413, ep5=0.8433) |
| P4-SEQ-2a | 时间周期特征 | T2 + use_time_period | 0.8683 | 0.8429/0.8401/0.8381 | -0.21/-0.59/-0.80% | ❌ 全负向 (Valid 高但 Eval 低，严重过拟合) |
| P4-U-D1 | Dense MLP | T2 + use_dense_mlp + bs48 | 0.86421 (ep4) | 0.843732 | -0.12% | ❌ 负向 (MLP 压缩不如单层) |
| P5-MLP1 | Deep Classifier | T2 + use_deep_classifier | 0.86308 (ep2) | 0.840612 | -0.43% | ❌ 负向 (bottleneck 不适用) |
| P5-M2 | HyFormer Blocks 1 | T2 + blocks=1 | 0.86545 (ep7) | — (权重未保存) | -0.04%* | ⚠️ Valid 负向 (blocks=1 不够) |
| P5-M3 | HyFormer Blocks 3 | T2 + blocks=3 | 0.86554 (ep4) | — (权重未保存) | -0.03%* | ⚠️ Valid 负向 (blocks=3 过拟合) |
| **P5.5-QRY1** | **Target-Aware Query** | **T2 + din_pool in QueryGen** | **0.86582 (ep8)** | **0.844115** | **-0.08%** | **❌ 接近但负向** |
| P5.5-DOM1 | 域级动态门控 | T2 + domain gate | 0.86481 (ep4) | 0.843100 | -0.19% | ❌ 负向 (gate 无正向收益) |
| P5.5-TEMP1 | 时间条件化门控 | T2 + temporal gate | 0.86502 (ep6) | 0.843035 | -0.19% | ❌ 负向 (乘法门控不如加法) |
| P4-U-D2 | Shared-FID 联合 | T2 + shared_fid_joint | 0.86500 (ep4) | 0.836210 | -0.87% | ❌ 严重负向 (交叉特征有害) |
| P4-SEQ1 | 移除 seq_c fid47 | T2 - seq_c_fid47 | - | 0.8375 | -0.75% | ❌ 严重负向 (fid47 不可移除) |

> *带 * 号为训练中间结果

---

### Phase3-ACCEL-Compile-v1: torch.compile (reduce-overhead)

**日期**: 2026-05-12
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_compile (torch.compile, mode="reduce-overhead")

#### 状态: ❌ FAILED (Segmentation fault)

#### 崩溃分析

```
训练第一步耗时 57 秒 (正常 ~0.5 秒)
警告: "skipping cudagraphs due to deterministic index put"
原因: CUDA Graphs (reduce-overhead 模式) 与模型的动态 embedding scatter/index 操作不兼容
具体: model.py line 1995 forward pass 中的 embedding lookup 使用了非确定性索引
结果: 第一步训练后 Segmentation fault (exit code 139)
```

#### 结论
- ❌ `mode="reduce-overhead"` 不可用 — 模型的 sparse embedding 机制与 CUDA Graphs 冲突
- 改用 `mode="default"` (仅算子融合，不使用 CUDA Graphs)

---

### Phase3-ACCEL-Compile-v2: torch.compile (default mode)

**日期**: 2026-05-12
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_compile (torch.compile, mode="default") + torch.set_float32_matmul_precision('high')

#### 状态: 🔄 训练中 (2 epochs 完成)

#### 实现要点
- `torch.compile(model, mode="default")` — 仅做算子融合，不使用 CUDA Graphs
- `torch.set_float32_matmul_precision('high')` — 启用 TF32 加速矩阵乘法
- 其他参数完全与 T2 baseline 相同

#### 训练中间结果

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.8620 |
| 2 | 0.8641 |

> 对比 T2: ep1=0.8619 (+0.01%), ep2=0.8641 (+0.02%) — 路径一致，AUC 匹配

---

### Phase3-ACCEL-BF16-v1: BF16 混合精度训练

**日期**: 2026-05-12
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_bf16 (torch.autocast with bfloat16)

#### 状态: ❌ FAILED

#### 崩溃分析

```
TypeError: Got unsupported ScalarType BFloat16
原因: 训练代码中调用 .numpy() 方法时，BF16 tensor 不支持直接转为 numpy
修复: 在调用 .numpy() 前先 .float() 转为 FP32
```

#### 结论
- ❌ BF16-v1 因 TypeError 失败，需要在代码中所有 `.numpy()` 调用前添加 `.float()`
- 已修复为 BF16-v2

---

### Phase3-ACCEL-BF16-v2: BF16 混合精度训练 (修复后)

**日期**: 2026-05-12 ~ 2026-05-13
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_bf16 (torch.autocast with bfloat16), 修复 `.numpy()` 调用

#### 状态: ✅ 已完成 (❌ 负向)

#### 实现要点
- `_train_step` 和 `_evaluate_step` 中使用 `torch.autocast(device_type="cuda", dtype=torch.bfloat16)`
- BF16 不需要 GradScaler（与 FP16 不同）
- 模型参数仍为 FP32，仅前向传播使用 BF16
- **修复**: 在所有 `.numpy()` 调用前添加 `.float()` 转为 FP32
- 其他参数完全与 T2 baseline 相同

#### 训练中间结果 (截至 ep5)

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.8619 |
| 2 | 0.8638 |
| 3 | 0.8648 |
| 4 | 0.8654 |
| **5** | **0.8663** |

> Best Valid AUC 0.8663 at step 157690 (epoch 5)

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 4 | 0.841327 | 88939 | -0.37% |
| epoch 5 | 0.84327 | 90879 | -0.17% |

#### 对比 T2

| 指标 | T2 (baseline) | BF16-v2 (ep5) | 差异 |
|------|---------------|---------------|------|
| Best Valid AUC | 0.8658 | **0.8663** | +0.05% |
| **Eval AUC** | **0.844955** | 0.84327 | **-0.17%** |
| Gap (Valid→Eval) | 2.08% | **2.30%** | +0.22% |

#### 结论
- ❌ **负向** — BF16 混合精度导致 Eval AUC 下降 (ep4 -0.37%, ep5 -0.17%)
- Valid AUC 略高 (+0.05%) 但 Eval AUC 下降更多，Gap 从 2.08% 增至 2.30%
- **BF16 不可用于竞赛提交**，精度损失不可逆
- **可用场景**: 仅用于加速实验迭代（非正式提交），预期 20-40% 加速

---

### Phase4-SEQ1: 移除 seq_c fid47 特征

**日期**: 2026-05-15 ~ 2026-05-20
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +seq_c_fid47_removed (移除 seq_c 域 fid47 相关特征)

#### 状态: ❌ 完成 (严重负向)

#### run.sh 语法错误修复
- 初始提交失败: bash 语法错误 `line 63: unexpected end of file`
- 问题原因: line 62 有尾部反斜杠 `\`，但 line 63 关闭参数列表为 `"$@"`
- 修复方案: 删除 line 62 的尾部反斜杠，因为 `"$@"` 表示参数列表结束
- 已修复并重新准备提交

#### 实验目的
- 验证移除 fid47 特征是否影响模型性能
- fid47 可能存在噪声或不相关信息
- 期待减少过拟合，提升 Eval AUC

#### 推理评估

| 指标 | T2 (baseline) | SEQ1-fid47Treatment | 差异 |
|------|---------------|---------------------|------|
| **Eval AUC** | **0.844955** | 0.8375 | **-0.75%** |

> epoch 4 checkpoint, Eval AUC = 0.8375

#### 结论
- ❌ **严重负向** — 移除 seq_c fid47 特征导致 Eval AUC 大幅下降 (0.844955 → 0.8375, -0.75%)
- **fid47 是 seq_c 中极其重要的特征**: 移除后性能暴跌，说明该特征携带了关键的用户行为信息
- **社区情报验证**: "seqc_feat47 跟 itemid 是一样的"——虽然它是 ID 类特征，但在 seq_c 域中它的 match/attention 机制对模型预测至关重要
- **与预期相反**: 原本期待移除 ID 特征能减少过拟合、提升泛化，但实际上 fid47 在序列建模中提供了不可替代的 target-item matching 信号
- **不纳入 baseline**

---

### Phase4-SEQ2a: 时间周期特征 (hour_of_day + day_of_week)

**日期**: 2026-05-13
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_time_period (gated additive residual, hour_of_day + day_of_week sinusoidal encoding)

#### 状态: ❌ 负向

#### 推理评估

| 指标 | T2 (baseline) | SEQ2a (ep4) | 差异 |
|------|---------------|-------------|------|
| **Eval AUC** | **0.844955** | 0.842944 | **-0.21%** |

> epoch 4 checkpoint, evalTaskId 91069

#### 完整评估结果

| Epoch | Valid AUC | Eval AUC | evalTaskId | Gap | 对比 T2 Eval |
|-------|-----------|----------|------------|-----|-------------|
| 1 | 0.86342 | - | - | - | - |
| 2 | 0.86500 | - | - | - | - |
| 3 | 0.86583 | - | - | - | - |
| 4 | 0.86652 | 0.842944 | 91069 | **-2.39%** | -0.21% |
| 5 | 0.86778 | - | - | - | - |
| 6 | 0.86771 | - | - | - | - |
| 7 | 0.86814 | 0.84009 | 92237 | **-2.80%** | -0.59% |
| **8** | **0.86829** | 0.838095 | 92286 | **-2.92%** | -0.80% |

> **Best Valid AUC = 0.86829 at Epoch 8**，高于 T2 的 0.8658 (+0.25%)

#### 结论
- ❌ **严重负向** — 时间周期特征 (hour_of_day + day_of_week sinusoidal encoding) 极度过拟合
- **Valid AUC 高但 Eval AUC 低**: Best Valid AUC 0.8683 > T2 (0.8658)，但 Eval AUC 全部低于 T2
- **Gap 异常增大**: ep4 Gap = -2.39%, ep7 Gap = -2.80%, ep8 Gap = -2.92% (正常应该是正数，说明 Valid > Eval)
- **Epoch 越后过拟合越严重**: ep4 Eval 0.8429, ep7 Eval 0.8401, ep8 Eval 0.8381，持续下降
- **特征无效且有害**: 用户日内/周行为模式对该 PCVR 任务不仅无效，还会引入严重过拟合
- gated additive residual 融合方式可能引入了噪声，但核心问题是时间周期特征本身不适合该任务
- 不纳入 baseline
- **SEQ2a 全部 epoch 均负向，时间周期特征验证失败**

---

### Phase4-U-D1: User Dense 增强投影 (2-layer MLP)

**日期**: 2026-05-13 → 2026-05-19
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_dense_mlp (user_dense_proj: 918→128→64 2-layer MLP, 替代 918→64 单层)

#### 状态: ❌ 完成

#### v1 经历
- v1 (batch_size=64): Epoch 1 Valid AUC 0.8628 完成，Epoch 2 开始 OOM
- 原因: MLP 中间激活 (d_model*2=128) 增加显存占用 + 共享 GPU 资源争抢
- v2 (batch_size=48): 降低 batch_size 增加显存余量，已重新提交

#### v2 训练结果 (最终)

| Epoch | Valid AUC | LogLoss |
|-------|-----------|---------|
| 1 | 0.86129 | - |
| 2 | 0.86299 | - |
| 3 | 0.86326 | - |
| **4** | **0.86421** | - |

> **Best Valid AUC 0.86421 at Epoch 4**，与 T2 baseline 的 0.8658 相比略低 (-0.16%)

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 4 | **0.843732** | 94348 | **-0.12%** |

#### 对比 T2

| 指标 | T2 (baseline) | U-D1-v2 (ep4) | 差异 |
|------|---------------|---------------|------|
| Valid AUC | 0.8658 | 0.86421 | -0.16% |
| **Eval AUC** | **0.844955** | **0.843732** | **-0.12%** |
| Gap (Valid→Eval) | 2.08% | 2.15% | +0.07% |

#### 结论
- ⚠️ **轻负向** — 2-layer MLP 未能带来预期提升，Eval AUC 下降 0.12%
- **Valid AUC 略低**: 0.86421 vs T2 0.8658 (-0.16%)
- **Eval AUC 略低**: 0.843732 vs T2 0.844955 (-0.12%)
- **Gap 略增**: 从 2.08% 增至 2.15%
- **分析**: 虽然理论上 918→128→64 的渐进压缩应该保留更多信息，但实际效果不如单层投影
- **可能原因**: 2 层 MLP 增加了模型复杂度和参数量 (~126K)，但数据量不足以支持更复杂的表征学习
- **不纳入 baseline**，但优于大部分其他修改（BF16、Dropout、LabelSmoothing、LongTrain）

---

### Phase5-MLP1: Deep 3-layer Classifier (64→128→64→1)

**日期**: 2026-05-13 → 2026-05-19
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_deep_classifier (classifier: 3层 64→128→64→1, 替代 2层 64→64→1)

#### 状态: ❌ 完成

#### 实现要点
- Classifier 从 2层 (`Linear(64→64) → LN → SiLU → Dropout → Linear(64→1)`) 扩展为 3层
- 新结构: `Linear(64→128) → LN → SiLU → Dropout → Linear(128→64) → LN → SiLU → Linear(64→1)`
- 中间层扩展到 128 维，引入 bottleneck 结构
- 其他参数完全与 T2 baseline 相同

#### 训练结果 (最终)

| Epoch | Valid AUC | LogLoss |
|-------|-----------|---------|
| 1 | 0.86203 | - |
| **2** | **0.86308** | - |

> **Best Valid AUC 0.86308 at Epoch 2**，与 T2 baseline 的 0.8658 相比略低 (-0.29%)

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 2 | **0.840612** | 94315 | **-0.43%** |

#### 对比 T2

| 指标 | T2 (baseline) | MLP1 (ep2) | 差异 |
|------|---------------|-----------|------|
| Valid AUC | 0.8658 | 0.86308 | -0.29% |
| **Eval AUC** | **0.844955** | **0.840612** | **-0.43%** |
| Gap (Valid→Eval) | 2.08% | 2.52% | +0.44% |

#### 结论
- ❌ **负向** — 3 层 Classifier 导致 Eval AUC 显著下降 (-0.43%)
- **Valid AUC 较低**: 0.86308 vs T2 0.8658 (-0.29%)
- **Eval AUC 显著降低**: 0.840612 vs T2 0.844955 (-0.43%)
- **Gap 增大**: 从 2.08% 增至 2.52%
- **分析**: 加深 Classifier 增加了模型复杂度，但在输出层附近的复杂结构容易导致过拟合
- **可能原因**: bottleneck 结构 (64→128→64) 在分类任务中不适用，反而增加了噪声
- **不纳入 baseline**，表现劣于 Phase4-U-D1 的 2-layer MLP

---

### Phase5-M2: HyFormer Blocks 1

**日期**: 2026-05-14
**基于版本**: T2 baseline (cosine LR)
**改动变量**: num_hyformer_blocks: 2 → 1
**任务ID**: f23eb273

#### 状态: ✅ 训练完成 (⚠️ 权重未保存，无法评估推理 AUC)

#### 实现要点
- 减少模型复杂度，测试更少的 HyFormer Block 是否减少过拟合
- 从 2 层 HyFormer Block 减少到 1 层
- 其他参数完全与 T2 baseline 相同
- 风险等级: 低（仅超参数修改）

#### 实验目的
- 验证是否"少即是更多"，避免模型过度复杂导致的过拟合
- 与 T2 (blocks=2) 和 M3 (blocks=3) 形成完整对比
- 确定最优的 HyFormer Block 数量

#### 训练结果 (8 epochs, 全部完成)

| Epoch | Valid AUC | LogLoss |
|-------|-----------|---------|
| 1 | 0.86229 | 0.23058 |
| 2 | 0.86399 | 0.22868 |
| 3 | 0.86404 | 0.22707 |
| 4 | 0.86460 | 0.22575 |
| 5 | 0.86540 | 0.22499 |
| 6 | 0.86541 | 0.22459 |
| **7** | **0.86545** | **0.22452** |
| 8 | 0.86490 | 0.22507 |

**Best Valid AUC**: 0.8654 (Epoch 7, step 220766)
**对比 T2 (0.8658)**: -0.04%

#### 分析
- Best Valid AUC 0.8654 < T2 的 0.8658，减少 HyFormer Block 数量导致表示能力不足
- 训练收敛正常（8 epochs 全部完成），但 Valid AUC 始终低于 T2
- **由于权重未保存到模型，无法提交推理评估**，仅能基于 Valid AUC 判断为**轻负向**
- 结论: 1 个 HyFormer Block 不够，2 个是更优配置

---

### Phase5-M3: HyFormer Blocks 3

**日期**: 2026-05-14
**基于版本**: T2 baseline (cosine LR)
**改动变量**: num_hyformer_blocks: 2 → 3
**任务ID**: cf95bb6f

#### 状态: ✅ 训练完成 (⚠️ 权重未保存，无法评估推理 AUC)

#### 实现要点
- 增加模型容量，测试更多层是否提升表示能力
- 从 2 层 HyFormer Block 增加到 3 层
- 其他参数完全与 T2 baseline 相同
- 风险等级: 低（仅超参数修改）

#### 实验目的
- 验证增加模型深度是否能捕获更复杂的特征交互
- 与 T2 (blocks=2) 和 M2 (blocks=1) 形成完整对比
- 确定最优的 HyFormer Block 数量

#### 训练结果 (7 epochs, Best at Epoch 4 后 patience=3 早停)

| Epoch | Valid AUC | LogLoss |
|-------|-----------|---------|
| 1 | 0.86221 | 0.22974 |
| 2 | 0.86391 | 0.22787 |
| 3 | 0.86384 | 0.22729 |
| **4** | **0.86554** | **0.22538** |
| 5 | 0.86530 | 0.22483 |
| 6 | 0.86549 | 0.22467 |
| 7 | 0.86526 | 0.22472 |

**Best Valid AUC**: 0.8655 (Epoch 4, step 126152)
**对比 T2 (0.8658)**: -0.03%

#### 分析
- Best Valid AUC 0.8655 < T2 的 0.8658，增加 HyFormer Block 到 3 层也未带来正向收益
- Epoch 4 达到峰值后 3 个 epoch 未再超过 (patience=3 触发)，说明更多层导致过拟合加剧
- **由于权重未保存到模型，无法提交推理评估**，仅能基于 Valid AUC 判断为**微弱负向**
- 结论: 3 个 HyFormer Block 过拟合风险更高，2 个是最优配置

---

### HyFormer Block 数量对比总结

| 实验 | Blocks | Best Valid AUC | 对比 T2 | 评估状态 |
|------|--------|---------------|---------|---------|
| M2 | 1 | 0.8654 | -0.04% | ⚠️ 无 Eval (权重未保存) |
| **T2** | **2** | **0.8658** | **baseline** | ✅ Eval 0.8450 |
| M3 | 3 | 0.8655 | -0.03% | ⚠️ 无 Eval (权重未保存) |

**结论**: num_hyformer_blocks=2 是最优配置，减少或增加都不如当前设置。社区情报"pure model 建模掉分"再次得到验证 — 即使是简单的超参调整也无收益。

---

### Phase3.5-GAP4-SWA: Stochastic Weight Averaging (SWA) + GAP4

**日期**: 2026-05-14
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +SWA (Stochastic Weight Averaging over last 4 epochs)

#### 状态: ⚠️ 负向

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 2 | 0.836432 | 96152 | -0.91% |
| epoch 3 | 0.837357 | 112009 | -0.80% |
| **epoch 4** | **0.840984** | 112014 | **-0.40%** |

> **Best Eval AUC 0.840984 at Epoch 4**，与 T2 baseline 的 0.844955 相比 (-0.40%)

#### 对比 T2

| 指标 | T2 (baseline) | SWA (ep4) | 差异 |
|------|---------------|-----------|------|
| **Eval AUC** | **0.844955** | **0.840984** | **-0.40%** |

#### 结论
- ⚠️ **负向** — SWA 未能带来预期提升，Eval AUC 下降 0.40%
- **Eval AUC 递增**: ep2 0.836432 < ep3 0.837357 < ep4 0.840984，显示 SWA 平均效果逐渐提升
- **但仍低于 T2**: 即使最佳 epoch 4，仍低于 T2 baseline
- **分析**: SWA 理论上能提升泛化能力，但可能不适用于此任务或当前训练策略
- **可能原因**: Cosine LR scheduler 本身已有平滑效果，SWA 可能引入额外噪声
- **不纳入 baseline**

---

### Phase5.5-QRY1: Target-Aware Query Token Interface

**日期**: 2026-05-20 ~ 2026-05-21
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_target_aware_query (DIN-style activation unit in Query Generator)

#### 状态: ❌ 完成 (接近但负向)

#### 实现要点
- 在 `MultiSeqQueryGenerator` 中添加 DIN-style target-conditioned pooling
- 新增 `DINActivationUnit`: `[seq, target, seq-target, seq*target]` → FFN → attention weights
- Query token 从"无 target 感知"变为"target-aware"，理论上让 query 在序列编码阶段就知道要预测什么
- 每个行为域独立计算 DIN activation，增强跨域信息融合
- 其他参数完全与 T2 baseline 相同

#### 训练结果 (8 epochs, 全部完成)

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.86200 |
| 2 | 0.86333 |
| 3 | 0.86405 |
| 4 | 0.86461 |
| 5 | 0.86514 |
| 6 | 0.86539 |
| 7 | 0.86529 |
| **8** | **0.86582** |

**Best Valid AUC**: 0.86582 (Epoch 8)
**对比 T2 (0.8658)**: +0.002% (几乎持平)

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 5 | **0.844115** | — | **-0.08%** |
| epoch 6 | 0.839362 | — | -0.56% |

#### 对比 T2

| 指标 | T2 (baseline) | QRY-1 (ep5) | 差异 |
|------|---------------|-------------|------|
| Best Valid AUC | 0.8658 | **0.86582** | +0.002% |
| **Eval AUC** | **0.844955** | **0.844115** | **-0.08%** |
| Gap (Valid→Eval) | 2.08% | 2.10% | +0.02% |

#### 结论
- ❌ **接近但负向** — Target-Aware Query 是所有实验中最接近 T2 的，但 Eval AUC 仍低 0.08%
- **Valid AUC 几乎一致**: 0.86582 vs 0.8658 (+0.002%)，说明 DIN activation 没有显著改变训练质量
- **Eval AUC 略低**: ep5 Eval 0.844115 vs T2 0.844955 (-0.08%)
- **ep6 严重退化**: Eval 0.839362，说明 DIN 引入的额外参数在后期 epoch 开始过拟合
- **分析**: DIN activation 虽然让 query token 对 target 有感知，但 HyFormer blocks 内部的 cross-attention 机制本身已经在做 target-seq 交互，DIN 的引入可能与现有机制存在冗余
- **不纳入 baseline**

---

### Phase5.5-DOM1: 域级动态可靠性门控

**日期**: 2026-05-20 ~ 2026-05-21
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_domain_gate (DomainReliabilityGate, per-domain sigmoid gate)

#### 状态: ❌ 完成 (负向)

#### 实现要点
- 新增 `DomainReliabilityGate` 类: 对 4 个行为域 (seq_a/b/c/d) 分别计算域级门控
- 门控机制: `score = dot_product(domain_proj(pooled_repr), context_proj(item_context)) + bias`
- `gated_seq = seq * (2.0 * sigmoid(score))` — 2x 缩放确保门控开启时信息量不变
- 在 HyFormer blocks dropout 之后应用域级门控
- 其他参数完全与 T2 baseline 相同

#### Bug 修复记录
- **v1 失败**: `TypeError: ~ (operator.invert) is only implemented on integer and Boolean-type tensors`
  - 原因: dropout 后 mask 变为 float 类型，`~` 运算符不支持 float tensor
  - 修复: `~seq_masks_list[i].unsqueeze(-1).float()` → `(1.0 - seq_masks_list[i].float()).unsqueeze(-1)`
- v2 修复后重新提交，训练成功

#### 训练结果 (v2, 仍在运行，截至 ep4)

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.86170 |
| 2 | 0.86384 |
| 3 | 0.86441 |
| **4** | **0.86481** |

**Best Valid AUC**: 0.86481 (Epoch 4)
**对比 T2 (0.8658)**: -0.10%

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 4 | **0.843100** | — | **-0.19%** |

#### 对比 T2

| 指标 | T2 (baseline) | DOM-1 (ep4) | 差异 |
|------|---------------|-------------|------|
| Best Valid AUC | 0.8658 | 0.86481 | -0.10% |
| **Eval AUC** | **0.844955** | **0.843100** | **-0.19%** |
| Gap (Valid→Eval) | 2.08% | 2.17% | +0.09% |

#### 结论
- ❌ **负向** — 域级动态门控未能带来正向收益，Eval AUC 下降 0.19%
- **Valid AUC 较低**: 0.86481 vs T2 0.8658 (-0.10%)
- **Gap 略增**: 从 2.08% 增至 2.17%
- **分析**: 门控机制引入了额外的 domain_proj/context_proj 参数，增加了模型复杂度但未带来信息增益
- **可能原因**: 4 个行为域 (seq_a/b/c/d) 已经通过 HyFormer blocks 的 cross-attention 被隐式地动态加权，显式门控是冗余的
- **不纳入 baseline**

---

### Phase5.5-TEMP1: 时间条件化特征门控

**日期**: 2026-05-20 ~ 2026-05-21
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +use_temporal_gate (multiplicative temporal gate, 替代 additive time_bucket)

#### 状态: ❌ 完成 (负向)

#### 实现要点
- 在 `_embed_seq_domain` 中，将时间特征的融合方式从加法改为乘法门控
- 原始: `token_emb = token_emb + time_emb` (additive)
- 新: `gate = nn.Sequential(nn.Linear(d_model, d_model), nn.Sigmoid())(time_emb)` → `token_emb = token_emb * gate`
- 乘法门控理论上让时间信息动态调制特征重要性
- 其他参数完全与 T2 baseline 相同

#### 训练结果 (8 epochs, 全部完成)

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.86062 |
| 2 | 0.86148 |
| 3 | 0.86324 |
| 4 | 0.86373 |
| 5 | 0.86464 |
| **6** | **0.86502** |
| 7 | 0.86498 |
| 8 | 0.86441 |

**Best Valid AUC**: 0.86502 (Epoch 6)
**对比 T2 (0.8658)**: -0.08%

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 5 | **0.843035** | — | **-0.19%** |

#### 对比 T2

| 指标 | T2 (baseline) | TEMP-1 (ep5) | 差异 |
|------|---------------|-------------|------|
| Best Valid AUC | 0.8658 | 0.86502 | -0.08% |
| **Eval AUC** | **0.844955** | **0.843035** | **-0.19%** |
| Gap (Valid→Eval) | 2.08% | 2.20% | +0.12% |

#### 结论
- ❌ **负向** — 乘法时间门控不如加法融合，Eval AUC 下降 0.19%
- **Valid AUC 较低**: 0.86502 vs T2 0.8658 (-0.08%)
- **Gap 增大**: 从 2.08% 增至 2.20%
- **分析**: 乘法门控 `token * sigmoid(Linear(time))` 比 `token + time` 更激进，sigmoid 可能过度抑制某些特征
- **可能原因**: 加法融合是残差连接形式，梯度流动更稳定；乘法门控引入非线性但可能破坏了特征空间
- **不纳入 baseline**

---

### Phase4-U-D2: Shared-FID 联合建模

**日期**: 2026-05-19 ~ 2026-05-21
**基于版本**: T2 baseline (cosine LR)
**改动变量**: +shared_fid_joint (所有序列域共享 FID embedding + 显式交叉特征)

#### 状态: ❌ 完成 (严重负向)

#### 实现要点
- 将 4 个行为域的 FID embedding 从独立变为共享
- 添加显式交叉特征 (user_dense × item_context, seq_domain × item_context)
- 目标: 让不同域的 FID embedding 互相增强，并捕获显式交叉信号
- 其他参数完全与 T2 baseline 相同

#### 训练结果 (截至 ep4)

| Epoch | Valid AUC |
|-------|-----------|
| 1 | 0.86199 |
| 2 | 0.86319 |
| 3 | 0.86456 |
| **4** | **0.86500** |

**Best Valid AUC**: 0.86500 (Epoch 4)
**对比 T2 (0.8658)**: -0.08%

#### 推理评估

| Checkpoint | Eval AUC | evalTaskId | 对比 T2 |
|-----------|----------|------------|---------|
| epoch 4 | **0.836210** | — | **-0.87%** |

#### 对比 T2

| 指标 | T2 (baseline) | U-D2 (ep4) | 差异 |
|------|---------------|-----------|------|
| Best Valid AUC | 0.8658 | 0.86500 | -0.08% |
| **Eval AUC** | **0.844955** | **0.836210** | **-0.87%** |
| Gap (Valid→Eval) | 2.08% | 2.88% | +0.80% |

#### 结论
- ❌ **严重负向** — Shared-FID 联合建模导致 Eval AUC 大幅下降 0.87%
- **Valid AUC 尚可**: 0.86500 仅比 T2 低 0.08%
- **Gap 暴增**: 从 2.08% 增至 2.88%，严重过拟合
- **分析**: 共享 FID embedding 破坏了各行为域的独立表征能力，交叉特征引入了过多噪声
- **与 SEQ1 一致**: fid47 的处理方式对模型影响巨大，不可随意修改
- **不纳入 baseline**

---

### Phase 5.5 YAO 洞察实验总结

| 实验 | 核心思路 | Best Valid | Eval AUC | 对比 T2 | Gap | 结论 |
|------|---------|-----------|----------|---------|-----|------|
| QRY-1 | DIN target-aware query | 0.86582 | 0.844115 | **-0.08%** | 2.10% | 最接近，但 DIN 冗余 |
| DOM-1 | 域级动态门控 | 0.86481 | 0.843100 | -0.19% | 2.17% | 门控冗余，增加复杂度 |
| TEMP-1 | 乘法时间门控 | 0.86502 | 0.843035 | -0.19% | 2.20% | 乘法不如加法 |
| U-D2 | Shared-FID 联合 | 0.86500 | 0.836210 | **-0.87%** | 2.88% | 严重负向 |

**关键洞察**: YAO 的三个架构优化方向 (target-aware query, domain gating, temporal gating) 全部负向。这表明在当前 HyFormer 框架内，信息流已经足够高效，额外的门控/注意力机制只会增加参数量和过拟合风险。

---

## 16. 全面负向分析与新优化方向 🆕

> **分析时间**: 2026-05-21 | 基于 20+ 消融实验的全部结果

### 16.1 核心问题诊断

**事实**: 20+ 个控制变量实验中，仅 T2 (Cosine LR) 实现了正向突破 (+0.52%)，其余全部负向。

**诊断**: 问题不在模型结构、不在训练策略、不在特征工程——**问题在于 Valid→Eval 的泛化 Gap**。

```
实验数据点:
- T2:          Gap = 2.08% (最佳)
- T6a:         Gap = 2.11% (微弱负向)
- QRY-1:       Gap = 2.10% (接近)
- U-D1:        Gap = 2.15%
- DOM-1:       Gap = 2.17%
- TEMP-1:      Gap = 2.20%
- T5:          Gap = 2.30%
- BF16:        Gap = 2.30%
- T9:          Gap = 2.31%
- T4b:         Gap = 2.46%
- SEQ2a:       Gap = 2.39%~2.92% (递增)
- U-D2:        Gap = 2.88% (严重)
```

**核心规律**:
1. **Valid AUC 不是问题** — 大部分实验的 Valid AUC ≥ 0.865，甚至高于 T2 的 0.8658
2. **Eval AUC 才是瓶颈** — 所有实验的 Eval AUC 都 < 0.8450
3. **任何增加模型复杂度的改动都增大 Gap** — 参数增加 → Valid 略高但 Eval 更低
4. **T2 的成功是"减法"** — Cosine LR 不增加参数，只优化了训练动态

### 16.2 Gap 的根本原因分析

| 假说 | 证据 | 可行性 |
|------|------|--------|
| **验证集分布偏差** | Valid 集来自训练尾部 5%，与测试集时间分布不同 | 不可控（平台决定测试集） |
| **ID 特征过拟合** | SEQ1 (-0.75%), 社区情报 "ID 特征线上掉分" | 高 |
| **时间漂移** | 测试集比训练集更新，分布偏移 | 中 |
| **模型过参数化** | 所有增加参数的改动都负向 | 中 |
| **训练数据量不足** | 社区情报 "数据量不够" | 不可控 |

### 16.3 新优化方向建议

基于以上分析，提出以下 **高概率正向** 的优化方向：

#### 方向 A: 更激进的 LR 策略探索 (P0 优先级) 🎯

**理由**: T2 证明 LR 策略是唯一有效的优化维度。应在此方向做更深入的搜索。

| 子实验 | 改动 | 理由 |
|--------|------|------|
| **LR-1: Warmup 调优** | warmup_steps: 500 → 300/700/1000 | 当前 warmup=500 未经验证，可能有更优值 |
| **LR-2: Cosine 参数调优** | min_lr_ratio: 0.1 → 0.05/0.2 | 控制学习率衰减终点 |
| **LR-3: Polynomial Decay** | lr_scheduler=polynomial, power=2 | 比 cosine 更平缓的衰减曲线 |
| **LR-4: OneCycleLR** | max_lr=5e-4, pct_start=0.3 | 超级收敛策略，可能进一步缩小 Gap |
| **LR-5: WarmupCosineDecay** | warmup + cosine + final restart | 在训练末期重启学习率避免过拟合 |

**预期收益**: +0.05%~0.20% Eval AUC
**风险**: 极低 — 不增加模型参数，仅调整训练动态
**优先级**: **P0 — 最有可能突破的方向**

#### 方向 B: 精细化 ID 特征处理 (P1 优先级)

**理由**: SEQ1 证明 fid47 不可移除但社区说它"线上掉分"，矛盾暗示需要**降权**而非移除。

| 子实验 | 改动 | 理由 |
|--------|------|------|
| **ID-1: FID Hash Bucket** | 对 ID 类特征使用 hash bucket (size=1000) 替代直接 embedding | 限制 ID 特征的记忆能力 |
| **ID-2: FID 学习率降权** | ID 类特征 sparse_lr 从 0.05 → 0.01 | 减缓 ID 特征的过拟合速度 |
| **ID-3: FID Weight Decay** | ID 类特征增加 weight_decay=0.1 | 惩罚 ID 特征的大权重 |

**预期收益**: +0.10%~0.30% Eval AUC
**风险**: 中 — 需要识别哪些是 ID 类特征
**优先级**: P1

#### 方向 C: Checkpoint 选择策略优化 (P1 优先级)

**理由**: QRY-1 的 ep5 Eval 0.8441 vs ep6 Eval 0.8394，差距巨大。说明 **选择哪个 checkpoint 至关重要**。

| 子实验 | 改动 | 理由 |
|--------|------|------|
| **CKPT-1: 保存所有 epoch** | 保存每个 epoch 的 checkpoint | 当前只保存 best Valid AUC，可能错过 Eval 最优 |
| **CKPT-2: Early stop on LogLoss** | 用 LogLoss 而非 AUC 做 early stopping | LogLoss 对概率校准更敏感，可能选出更好的 checkpoint |
| **CKPT-3: Patience=2** | patience 从 3 → 2 | 更早停止防止过拟合 |

**预期收益**: +0.05%~0.15% Eval AUC
**风险**: 极低 — 不改变模型，只改变选择策略
**优先级**: P1

#### 方向 D: 序列长度调优 (P2 优先级)

**理由**: 当前 seq_top_k=50 是 v3 默认值，未经实验验证。序列长度直接影响模型复杂度和过拟合风险。

| 子实验 | 改动 | 理由 |
|--------|------|------|
| **SEQ-L1: 更短序列** | seq_top_k: 50 → 30 | 减少序列长度，降低过拟合风险 |
| **SEQ-L2: 更长序列** | seq_top_k: 50 → 80 | 增加行为信息量 |

**预期收益**: +0.0%~0.15% Eval AUC
**风险**: 低
**优先级**: P2

### 16.4 推荐实验优先级排序

```
Phase 6: LR 策略深度搜索 (P0, 最有可能突破)
├── LR-1: Warmup 调优 (300/700/1000)
├── LR-2: min_lr_ratio 调优 (0.05/0.2)
├── LR-3: Polynomial Decay
└── LR-4: OneCycleLR

Phase 7: ID 特征降权 (P1)
├── ID-1: Hash Bucket
├── ID-2: 学习率降权
└── ID-3: Weight Decay

Phase 8: Checkpoint 策略 (P1, 可与 Phase 6 并行)
├── CKPT-1: 保存所有 epoch
├── CKPT-2: LogLoss early stopping
└── CKPT-3: Patience=2
```

### 16.5 为什么之前的方向全部失败？

| 方向 | 失败原因 | 核心教训 |
|------|---------|---------|
| Dropout/LS/FeatMask (Phase2) | 正则化方式不当，增大 Gap | "更多正则化 ≠ 更好泛化" |
| 更长训练 (T9) | 过拟合验证集 | "更多 epoch ≠ 更好模型" |
| BF16 (Phase3) | 精度损失不可逆 | "速度不应牺牲精度" |
| 时间周期 (SEQ2a) | 无用特征引入噪声 | "不是所有特征都有用" |
| Dense MLP / Deep Classifier | 增加参数但数据不足 | "更多参数 ≠ 更强模型" |
| HyFormer Blocks ±1 | blocks=2 已是最优 | "当前架构已接近最优" |
| Target-Aware Query (QRY-1) | 与现有 DIN 冗余 | "信息流已足够高效" |
| Domain Gate (DOM-1) | 与 cross-attention 冗余 | "动态加权已在隐式发生" |
| Temporal Gate (TEMP-1) | 乘法破坏残差结构 | "加法融合更稳定" |
| Shared-FID (U-D2) | 破坏独立表征 | "域隔离是必要的" |

**统一解释**: **T2 的 HyFormer 架构 + Cosine LR 已经是该模型在当前数据量下的近最优解。进一步的提升必须从不同维度切入——训练动态 (LR 策略) 和数据/特征处理 (ID 特征降权)，而非继续修改模型结构。**

---

## 附录 A: V3 模型架构图

```
输入: user_int + user_dense + item_int + 4 序列域
  │
  ├── NS Tokenizer (RankMixer)
  │   ├── user_int → 5 NS tokens (B, 5, 64)
  │   ├── item_int → 2 NS tokens (B, 2, 64)
  │   └── user_dense → 1 token (B, 1, 64) [918→64, 压缩率14:1]
  │
  ├── 序列 Embedding (per domain, 4 域)
  │   ├── sideinfo emb + projection + time_bucket emb
  │   └── seq_a(256) + seq_b(256) + seq_c(512) + seq_d(512)
  │
  ├── Query Generator
  │   └── 每个 domain 生成 2 个 Q tokens = 8 Q tokens
  │
  ├── HyFormer Block × 2
  │   ├── 序列编码 (TransformerEncoder)
  │   ├── Cross Attention (Q → Seq)
  │   └── RankMixer (token mixing + FFN)
  │       └── T=16 tokens: 8Q + 5user_NS + 1user_dense + 2item_NS
  │
  └── 二阶段融合
      ├── Target Attention (DIN)
      ├── Context Fusion (残差)
      └── Cross Fusion (DCN-V2 低秩)
  │
  └── Classifier: Linear(64→64) + LN + SiLU + Dropout(0.01) + Linear(64→1)
```

## 附录 B: 关键约束

| 约束 | 值 | 说明 |
|------|-----|------|
| d_model % T == 0 | 64 % 16 == 0 ✓ | rank_mixer_mode='full' 的硬约束 |
| 禁止模型集成 | 单模型提交 | 赛规要求 |
| 推理延迟限制 | 严格限制 | 具体阈值未知 |
| T = num_queries×4 + num_ns | 当前 T=16 | 改 num_queries 或 num_ns 需重新计算 |
| item_dense 为空 | dim=0 | 物品侧无稠密特征 |

## 附录 C: 快速参数速查表

| 场景 | 参数 | 值 |
|------|------|-----|
| 关闭 sparse re-init | reinit_sparse_after_epoch | -1 |
| 选择性 re-init | reinit_sparse_after_epoch + reinit_cardinality_threshold | 2, 100000 |
| ~~开启 EMA~~ | ~~use_ema + ema_decay + ema_warmup_steps~~ | ~~❌ 不可用~~ |
| Cosine LR | lr_scheduler + warmup_steps + min_lr_ratio | cosine, 500, 0.1 |
| Label Smoothing | label_smoothing | 0.01 (❌ 负向 -0.34%, Gap 增大) |
| Feature Mask | feature_mask_ratio | 0.05 (微弱负向) |
| Dropout | dropout_rate | 0.02 (❌ 负向, 当前 0.01 最优) |
| 更长训练 | num_epochs + patience | 12, 5 (❌ 负向, 当前 8+3 最优) |
| torch.compile | use_compile | True (mode=default, 🔄 训练中 ep2 AUC 一致) |
| BF16 混合精度 | use_bf16 | True (❌ ep5 Eval 0.8433, 负向 -0.17%, 不可用于竞赛) |
| 时间周期特征 | use_time_period | True (❌ Eval 0.8429, 负向 -0.21%) |
| 移除 seq_c fid47 | seq_c_fid47_removed | True (❌ Eval 0.8375, 严重负向 -0.75%, fid47 不可移除) |
| SWA 权重平均 | use_swa | True (待实现, Phase 3.5) |
| Dense MLP 投影 | use_dense_mlp | True (🔄 v2 训练中, v1 OOM at ep2, batch 64→48) |

---

> **关键提醒**:
> 1. 当前最佳: T2 (cosine LR), Eval AUC = **0.844955**
> 2. **20+ 实验全部完成**: Phase0~5.5 共 20+ 个控制变量实验，仅 T2 (Cosine LR) 实现正向突破 (+0.52%)
> 3. **核心规律**: 任何增加模型复杂度的改动 (Dropout, LS, BF16, MLP, DIN, Gate, Cross) 都增大 Gap。T2 是在**不增加参数**的情况下通过优化训练动态实现的突破
> 4. **Phase 5.5 YAO 实验全部负向**: QRY-1 (-0.08%), DOM-1 (-0.19%), TEMP-1 (-0.19%), U-D2 (-0.87%)
> 5. **下一步方向 (Phase 6)**: 应转向 **LR 策略深度搜索** (P0) 和 **ID 特征降权** (P1)，而非继续修改模型结构
> 6. 社区情报表明数据量可能不够支撑复杂模型，保持简单
